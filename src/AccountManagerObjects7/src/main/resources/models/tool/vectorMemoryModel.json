{
	"name": "tool.vectorMemory",
	"inherits": ["data.vectorModelStore"],
	"description": "Vector store for memory chunks. Inherits embedding, chunk, content, vectorReference from data.vectorModelStore.",
	"fields": [
		{
			"name": "memoryType",
			"type": "string",
			"maxLength": 32,
			"description": "Denormalized from tool.memory for search filtering"
		},
		{
			"name": "conversationId",
			"type": "string",
			"maxLength": 128,
			"description": "Denormalized from tool.memory for conversation scoping"
		},
		{
			"name": "personId1",
			"type": "long",
			"description": "Denormalized from tool.memory for person pair filtering"
		},
		{
			"name": "personId2",
			"type": "long",
			"description": "Denormalized from tool.memory for person pair filtering"
		},
		{
			"name": "personModel",
			"type": "string",
			"maxLength": 64,
			"description": "Denormalized from tool.memory for model type filtering"
		}
	]
}
