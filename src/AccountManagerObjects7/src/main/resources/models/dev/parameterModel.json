{
	"name": "dev.parameter",
	"inherits": ["common.attribute"],
	"ioConstraints": ["unknown"],
	"fields": [
		{
			"name": "valueSchema",
			"type": "string",
			"description": "A JSON schema for the parameter value, for use when the entire object model may not be available."
		}
	]
}