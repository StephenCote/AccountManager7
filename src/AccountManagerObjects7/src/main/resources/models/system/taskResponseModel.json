{
	"name": "system.taskResponse",
	"inherits": ["system.taskRequest"],
	"ioConstraints": [
		"unknown"
	],
	"fields": [
		{
			"name": "responseType",
			"baseClass": "org.cote.accountmanager.schema.type.ResponseEnumType",
			"type": "enum",
			"maxLength": 10,
			"default": "UNKNOWN"
		}

	]
}