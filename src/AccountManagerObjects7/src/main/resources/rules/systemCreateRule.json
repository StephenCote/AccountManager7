{
	"urn": "resource.policy.rule.systemcreate",
	"type": "permit",
	"condition": "any",
	"name": "System Create Resource Access Rule", 
	"patterns": [
		"${pattern.ownerAccessOp}",
		"${pattern.groupOwnerAccessOp}",
		"${pattern.parentOwnerAccessOp}",
		"${pattern.accountAdministrator}",
		"${pattern.groupUpdateDataAccess}",
		"${pattern.groupUpdateAccess}",
		"${pattern.parentUpdateAccess}"
	]
}