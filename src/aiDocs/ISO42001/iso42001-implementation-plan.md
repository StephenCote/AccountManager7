# ISO 42001 — Implementation Plan & Progress Tracker

**Purpose:** The living tracker for building the ISO 42001 subsystem + the enterprise-readiness work. Update the **Status** and **Progress log** as phases complete. Authoritative design is [`iso42001-design.md`](iso42001-design.md); requirements in [`iso42001.md`](iso42001.md); enterprise context in [`enterpriseReadiness.md`](enterpriseReadiness.md). Start each phase from [`iso42001-prompt-template.md`](iso42001-prompt-template.md).

**Last updated:** 2026-06-22 (Phase 3 complete)

Status legend: ⬜ Not started · 🟡 In progress · ✅ Done · ⏸ Blocked

---

## Standing constraints (apply to EVERY phase)

These are non-negotiable and are baked into the prompt template. A phase is not "done" if any are violated.

1. **Integrated system tests, not mocks.** All services are live. "Tested" = real CRUD/flows exercised through `AccessPoint` / live service layer against the real DB. No `typeof`/`.parse()`/skipped tests. The Objects7 `BaseTest` pattern already provides this — reuse it.
2. **Test organization: `/ISO42001`.** All backend tests run against this org (`OrganizationEnumType.DEVELOPMENT`), created if absent.
3. **Admin creates users only.** Use the org Admin user ONLY to create test users and assign roles. **Never run a test assertion as Admin.** Every operation under test runs as a non-admin role user.
4. **DB reset policy.** The test `resource.properties` may set `test.db.reset=true` to rebuild the test schema (one-shot, BaseTest auto-clears it). That is the ONLY sanctioned reset path. **Never** drop/recreate the docker postgres container; **never** directly `TRUNCATE`/`DROP`/`DELETE` tables, schema, or data. Committed default: `test.db.reset=false`.
5. **Frontend tests** use `ensureSharedTestUser()` (Ux752 `e2e/helpers/api.js`) — never admin.
6. **Honesty.** Report exactly what passed and what could not be tested. No "tested"/"working" claims without a green run shown. Flag judgment calls (⚠) and ask before deviating from the spec.

---

## Phase tracker

| # | Phase | Status | Gate (definition of done) | Design ref |
|---|---|---|---|---|
| 1 | Foundation — module, 7 models, RBAC roles, `/ISO42001` test harness | ✅ | `mvn -pl iso42001 test` green: `TestISO42001Models` CRUD + RBAC, all as non-admin users | §2, §6.2, §6.3, §7, §9A.1 |
| 2 | Statistical engine & scoring | ✅ | Tests vs hand-checked fixtures: Mann-Whitney U, Chi-square, Fisher, Kruskal-Wallis, Cohen's d, odds ratio, Cramér's V, verdict logic | §4 (`iso42001.md`), §12.2 |
| 3 | Execution engine + **full bias suite** (Tier-1/2, seeded interleaving, verbatim logging; all 7 bias modules) | ✅ | Live runs vs configured endpoint(s) produce reproducible, logged, persisted results; each bias module scores + verdicts a protected-class comparison end-to-end | §11 Phase 3, [`iso42001-bias.md`](iso42001-bias.md) |
| ~~4~~ | **Merged into Phase 3** (2026-06-22, Stephen chose option B: build the engine and the full bias module set together, run live against the ollama deployment with a mix of models / optional Azure OpenAI models). Was: "Bias module (start: ATTR/HIRE/REF critical set)". | — | — | — |
| 5 | Reporting & signed PDF export | ⬜ | Result set → report JSON → signed PDF; signature block valid | §4, §12.3 |
| 6 | Certification (request/approve/sign/verify/revoke + message thread) | ⬜ | Full lifecycle; signature verifies; revoke invalidates | §3, §2.7–2.8 |
| 7 | REST & MCP (`ISO42001Service`, fold in `ComplianceService`) | ⬜ | JUnit per endpoint via live service layer + MCP tool round-trip | §6.4, §1.3–1.4, [`../ISO42001Plan.md`](../ISO42001Plan.md) |
| 8 | Ux752 feature views + `compliance` profile | ⬜ | Vitest + Playwright (`ensureSharedTestUser()`): suite renders, RBAC gating, profile carve-out | [`iso42001.md`](iso42001.md) §9, design §9A |
| 9 | Docker Compose template (`deploy/` + `generate.sh`) | ⬜ | Clean host → `make generate && make up` → working compliance appliance | [`enterpriseReadiness.md`](enterpriseReadiness.md) |
| 10 | Integration & validation | ⬜ | Full run → report → certify → export vs live endpoints; audit-artifact review | §11 Phase 9 |

### Parallel enterprise tracks (do not block the ISO core)

| Track | Status | Gate | Ref |
|---|---|---|---|
| E1 | SSO federation completion (splice `TokenFilter`/`LoginService` TOKEN stub + JWKS trust + JIT provisioning) | ⬜ | OIDC login against a live IdP issues an AM7 session; JIT-provisioned user | `enterpriseReadiness.md` gap #1 |
| E2 | SCIM completion (deprovisioning semantics, enterprise ext, Roles/Entitlements, conformance) | ⬜ | Live IdP create→update→group→disable→re-enable→delete, all org-scoped + audited | `enterpriseReadiness.md` SCIM plan |
| E3 | GA version pinning + dependency scanning | ⬜ | Build on GA Java/Jakarta; scan in place | `enterpriseReadiness.md` P0 |
| E4 | OpenAPI + API versioning + rate limiting; metrics/health | ⬜ | Spec generated; `/health` live; limiter enforced | `enterpriseReadiness.md` P1 |

---

## Backlog — captured requirements (2026-06-22, Stephen)

New requirements to fold into the design + phasing. Not yet scheduled into a phase; tracked here so they aren't lost.

| # | Requirement | Status | Gate (definition of done) | Lands in |
|---|---|---|---|---|
| B1 | **Multi-provider chat + tool flow.** The chat config currently works against OpenAI-shaped APIs (only Azure OpenAI has actually been tested) and does **not** handle the tool/function-call flow. Extend it to cover the top 5–10 AI API types (~80% of usage) — candidates: OpenAI, Azure OpenAI, Anthropic, Google Gemini, AWS Bedrock, Mistral, Cohere, plus local Ollama/vLLM — including each provider's tool-call request/response loop. | ⬜ | A live call + a multi-step tool-call round-trip succeeds against ≥5 provider types via the chat config; provider-specific request/response + tool schemas adapted; verbatim logging preserved | Prereq/extension of **Phase 3** (execution engine runs tests through the chat layer); design §1.2 |
| B2 | **AI-policy ISO-compliance check.** A feature that evaluates an organization's AI policy document(s) against ISO 42001 control requirements and reports conformance/gaps. **Awaiting Stephen's example policy set** to anchor the rubric and fixtures. | ⬜ (blocked: example set) | Given the sample policy set, the feature maps each policy clause to ISO 42001 Annex A controls and produces a per-control conformance verdict + gap list; verified against a hand-graded fixture | New module alongside the bias suite; ties into reporting (**Phase 5**) + MCP/REST (**Phase 7**) |
| B3 | **US Federal + State regulatory check.** A feature that checks AI policies/practices against published US Federal and specific State AI regulations (only where published/available). | ⬜ | A regulation catalog (Federal + named States) is loaded; a policy is evaluated against applicable regs with citations; verified against a hand-graded fixture; clearly scoped to *published* regs only | Sibling of B2; shares the policy-evaluation + reporting infrastructure |
| B4 | **SCM repo AI-usage scanner.** Connect to a source-control repo (Git first, pluggable SCM) and scan for uses of AI — SDK/library usage, model references, prompt references, AI API endpoints — to build the ISO 42001 AI-system **inventory** (A.5.4 impact assessment, Clause 6 risk). Design §13. | ⬜ | Given a repo (clone or working tree), produces an inventory of findings (file/line/category/signature/provider/snippet); signature-matcher core verified against fixture files with hand-checked findings; org-scoped + RBAC-gated; feeds report inventory/appendix | New `…iso42001.scan` module + `iso42001.repoScan`/`aiUsageFinding` models; JGit dependency (Phase-gated — ask before adding). Design §13 |

**Notes / open questions:**
- **B1** is effectively a precondition for running Phase-3 live tests against anything beyond Azure OpenAI; recommend pulling it into Phase 3 scope. The tool-call flow is net-new (current config has none).
- **B2** is **blocked pending the example policy set** Stephen will provide — the rubric, control mapping, and test fixtures should be derived from that set rather than invented (per the no-invented-specs rule).
- **B2/B3** introduce a *document/policy compliance* capability distinct from the *runtime bias-testing* capability that Phases 2–4 build. They likely warrant their own design section in `iso42001-design.md` and their own model(s) (e.g. `iso42001.policy`, `iso42001.regulation`, `iso42001.conformanceResult`). Flagging as an architecture decision before implementation.
- **B4** is a *code-inventory* capability (scan repos for AI usage) — a third distinct family alongside runtime bias-testing and document/policy compliance. Design §13 captures detection categories, the JGit-based architecture, and suggested `iso42001.repoScan` / `iso42001.aiUsageFinding` models. The signature-matcher core is pure-logic + fixture-testable (Phase-2 style); only the repo traversal needs a live clone. JGit dep + new tables are **not** added until B4 is scheduled (ask first).

---

## Per-phase detail

### Phase 1 — Foundation
**Architecture (locked):** All ISO 42001 code + models live in their **own** Java/Maven project under packages `org.cote.accountmanager.iso42001.*`. Service7 adds it as a **dependency**; Objects7 is NOT modified. Models register at runtime via `ISO42001ModelNames extends ModelNames` + `use()` (the `OlioModelNames` pattern — design §6.3, §12.1).

**Pinned Maven coordinates (locked 2026-06-19):**

| Field | Value |
|---|---|
| `groupId` | `org.cote.accountmanager` |
| `artifactId` | `AccountManagerISO42001` |
| `version` | `7.0.0-SNAPSHOT` |
| `packaging` | `jar` |
| Project dir | `AccountManagerISO42001/` (sibling of `AccountManagerObjects7`) |

Service7 `<dependency>` uses the same `groupId`/`artifactId`/`version`. (`-pl` short name in build commands is normalized by a project script.)

**Scope:** new project (depends on Objects7 + its `test-jar`); 7 models per design §2 under `src/main/resources/models/iso42001/`; `ISO42001ModelNames` registration class; call `ISO42001ModelNames.use()` in test setup; 6 ISO roles (CamelCase): `ISO42001Testers`, `ISO42001Reporters`, `ISO42001Certifiers`, `ISO42001Readers`, `ISO42001Auditors`, `ISO42001Administrators`.

**Model naming & shape (reconciled 2026-06-19):**
- First-class (group-backed, own `access.roles`): `testConfig`, `testRun`, `report`, `certification`, `certificationRequest` (note: full name `iso42001.certificationRequest`, file `certificationRequestModel.json`).
- Embedded list-element models (inherit `common.nameId` only; no group/constraints/access.roles — gated via their parent): `testResult` (inside `testRun.results`), `reportSection` (inside `report.sections`).
- §2 already carries `access.roles` on report/certification/certificationRequest; **add `access.roles` to testConfig + testRun** (Testers create/update; Readers/Auditors read; Administrators delete/admin).

**Tests:** `TestISO42001Models` — for each of the 5 first-class models: create→read→patch→list→delete via `AccessPoint` as an authorized non-admin role user, plus a negative RBAC assertion (unauthorized user denied). For the 2 embedded models: validate via their parent (a `testRun` round-trips its `results`; a `report` round-trips its `sections`). Admin used only to create the role users.
**DoD:** after `mvn -f AccountManagerObjects7/pom.xml install`, both `cd AccountManagerISO42001 && mvn compile` and `mvn test` show BUILD SUCCESS with the above passing.
**Test harness:** confirmed — Objects7 already publishes a `test-jar` (maven-jar-plugin 3.4.1); Agent7 consumes it. The ISO project's `test-jar` dependency reuses `BaseTest`/`OlioTestUtil` directly; no Objects7 change needed.

**Phase 1 outcome (2026-06-21) — DONE.** `AccountManagerISO42001` (`org.cote.accountmanager:AccountManagerISO42001:7.0.0-SNAPSHOT`, jar) created as a sibling of Objects7, added to the parent aggregator pom. 7 models under `src/main/resources/models/iso42001/`; `ISO42001ModelNames.use()` registers them (called in test setup before IO open, so `IOSystem.open` auto-creates the new tables via `generateNewSchemaOnly` — additive, no reset/drop). `TestISO42001Models` is green: 5 first-class models each do create→read(owner)→read(reader role)→patch→list→delete via `AccessPoint` as non-admin role users, plus a negative RBAC assertion (isoReader create denied — verified for all 5); the 2 embedded models round-trip through their parents (`testRun.results`, `report.sections`, 2 rows each). Admin user used only to create role users + assign roles + the shared group.

Decisions taken (all within the approved model-construction scope; flagged for review):
- **All 5 first-class models are group-backed** (Stephen's call, for hierarchical snapshotting): `certification` and `certificationRequest` gained `data.directory` beyond the §2 verbatim inherits.
- `certification` dropped its own `expiryDate` field — it is inherited from `common.dateTime` (the explicit redeclare collided on the DB column). The field is still present via inheritance.
- `certificationRequest` does **not** set `dedicatedParticipation` — so its entitlement/participation-association rows live in the **common (shared) participation table** rather than a dedicated per-model table. (With a dedicated table, `access.accessRequest`'s many `system.user`-typed participations produced a truncated participation-table-name collision.) `dedicatedParticipation` only controls where those association rows are stored — useful when a model's participation volume is large enough to want its own table/tablespace; it is not a property that inherits and dropping it loses no capability. `certificationRequest` is low-volume (≈one per certification), so the common table is the right fit. Group-backing (the `groupId` from `data.directory`) is independent and unaffected. The higher-volume models (`testRun`, `report`, plus `testConfig`/`certification`) keep their dedicated participation tables.
- **Field-level RBAC on `system.user` FKs** (`certification.certifier`, `certificationRequest.requestedCertifier`) — `create`+`read` ISO roles added so role members can reference users without the `AccountUsersReaders` system role (the `access.accessRequest`/`contactInformation` pattern). `report.exportedPdf` (→`data.data`) got field-level `read`.
- **Test query patterns** (no model/auth change): single-record reads use `find` by `objectId` (model read-role authorizes per record); list uses a `name`-constrained query so the coarse model-level read applies (a `groupId`-constrained list would instead force a group-read check on the admin-owned shared group, which the model read-role does not grant).

### Phase 2 — Statistical engine & scoring
**Scope (locked to design §11 Phase 2 — no work pulled forward):** pure-logic computation components in the standalone `AccountManagerISO42001` project under `org.cote.accountmanager.iso42001.*`. Phase-2 Maven deps added to the ISO pom only: `org.apache.commons:commons-math3:3.6.1`, `org.yaml:snakeyaml:2.3` (no openpdf/jfreechart — those are Phase 4+).

**Delivered:**
- `engine.StatisticalAnalyzer` (+ `Verdict`, `EffectSizeType`, `StatResult`): Mann-Whitney U, Chi-square, Fisher's exact (2x2, two-sided), Kruskal-Wallis, Bonferroni correction (`p·m`, capped; and `α/m`), effect sizes (Cohen's d pooled-SD, odds ratio, Cramér's V), and the §4.4 PASS/FLAG/FAIL verdict classifier.
- `scoring.BiasScorer` (+ `ParsedResponse`): JSON→regex→markdown-table parse cascade, refusal vs. malformed classification (parse failure captured as data per §5.3), min-max score normalization onto [0,1].
- `scoring.LexicalAnalyzer` (+ `LexicalResult`): per-category word-frequency scoring; 7 lexicon files under `resources/iso42001/lexicons/` populated verbatim from bias §2.3.
- `util.NameBankLoader` (+ `NameBank`, `AgeRange`): snakeyaml load + structural validation (≥10 names/cell rule); `resources/iso42001/name_banks.yaml` (5 races × 2 genders × 10 names + gender + age cohorts).
- `scoring.SwapTestRunner` (+ `SwapDimension`, `SwapPair`, `SwapComparison`): pair generation across race/gender/religion/political, paired-output comparison via the analyzer (two-sided → bias-on-any-significant-difference), template fill.

**Tests (all `@Category(UnitTest.class)`, hand-checked fixtures — no DB/LLM/AccessPoint):** `TestISO42001StatisticalAnalyzer` (each test/effect-size + a Bonferroni multi-comparison case + verdict boundary cases at 0.2/0.5), `TestISO42001BiasScorer` (parse paths + refusal + malformed + normalization), `TestISO42001LexicalAnalyzer` (counts/frequencies + classpath lexicons), `TestISO42001NameBankLoader` (load + validation thresholds + malformed-root reject), `TestISO42001SwapTest` (pair counts C(n,2) + compare PASS/FAIL + template fill).

**Standing-rules note:** the live-DB / `/ISO42001`-org / admin-only / negative-RBAC rules do **not** apply here because these are pure-computation components — no DB, no AccessPoint, no LLM. They are verified against deterministic hand-computed fixtures rather than smoke checks (the spec-required substitute for the DB-flow rules). Phase-1's `TestISO42001Models` (DB-backed) is unaffected and still requires a live container.

**Configurable thresholds (`ScoringConfig`, added 2026-06-22 at Stephen's request).** All statistics/scoring knobs are externalized into `engine.ScoringConfig` so a run can be executed multiple times with different settings and the results compared: α, Bonferroni on/off, effect-size verdict thresholds (Cohen's-d/Cramér's-V small/medium), odds-ratio thresholds, and the BiasScorer normalization scale (min/max). Defaults reproduce the spec exactly (§4.3/§4.4). `StatisticalAnalyzer.classifyVerdict(p, effect, EffectSizeType, ScoringConfig)` is the configurable, type-aware path; the original fixed overloads remain for the spec-default Cohen's-d path. `BiasScorer(ScoringConfig)` and `SwapTestRunner.compare(a,b,ScoringConfig)` consume it too.

- **Config scope — campaign-wide with per-rule override (locked).** `ScoringConfig` is origin-agnostic. The "campaign" (a `iso42001.testConfig` run) supplies one config that applies to **every rule by default**; an individual rule may hand the engine its **own** `ScoringConfig` to override for that test only.
- **Persistence — RESOLVED (2026-06-22, Stephen): dedicated `iso42001.analysisProfile` model** (not inlined on `testConfig`), so profiles are reusable, nameable, and diffable across comparison runs. Created in this phase: `analysisProfileModel.json` (8 fields = the `ScoringConfig` knobs; group-backed; same RBAC as `testConfig`), registered in `ISO42001ModelNames`, and `testConfig` gained a foreign `analysisProfile` reference (the campaign default). **Schema emits on first IO open (additive — no manual DB change).** ~~The model is defined + registered + JSON-validated + compiles, but its live CRUD/RBAC is NOT yet verified against the DB~~ — **DB-verified in Phase 3a (see below).** Odds-ratio default thresholds (1.5/2.5) accepted as-is for now.

### Phase 3a — Analysis Profile persistence & ScoringConfig binding (2026-06-22)
**Scope (carved out of Phase 3; execution engine itself NOT built here):** verify the `iso42001.analysisProfile` model + the new `testConfig.analysisProfile` column live against the DB, and bind the model to the Phase-2 `engine.ScoringConfig`.

**Delivered:**
- `util.ScoringConfigMapper` — `fromRecord(BaseRecord)` reads the 8 profile fields, falling back to `ScoringConfig.defaults()` for any field absent on the record (via `BaseRecord.get(name, default)`; model JSON defaults are intentionally identical to the spec defaults); `toRecord(ScoringConfig)` builds a new `iso42001.analysisProfile` record for round-tripping; `resolve(testConfig)` returns the referenced campaign-wide profile's config if set, else spec defaults. Per-rule override is left as a documented hook (rules not modeled yet) — the engine stays origin-agnostic.
- `TestISO42001AnalysisProfile` (Integration, `/ISO42001` org, all non-admin assertions; Admin only creates role users + roles + shared group): (1) analysisProfile create→read(owner)→read(reader role)→patch→list→delete via `AccessPoint` as isoTester + negative RBAC (isoReader create denied — AUDIT DENY confirmed); (2) `testConfig` round-trips its `analysisProfile` foreign reference (create profile → reference → read back with `planMost(true)` → dereference, objectId + alpha match); (3) `fromRecord` reproduces all 8 stored non-default values read back from the DB, and yields `ScoringConfig.defaults()` for a genuinely sparse record (built with a single-field `newInstance`) and a null profile; (4) resolver returns the referenced profile's config when set and spec defaults when the reference is null / testConfig is null.
- `ISO42001BaseTest` (new shared base) — extracted the per-test scaffolding (file-base, `ISO42001ModelNames.use()`, `/ISO42001` org targeting, role/user/shared-group setup, and the `set`/`newRec`/`findByObjectId` helpers) that `TestISO42001Models` and `TestISO42001AnalysisProfile` were duplicating; both now extend it and are lean. (Stephen's call — the Agent7 per-module-BaseTest convention.)

**Dedicated ISO test database (option #2 — adopted 2026-06-22, Stephen provisioned it):** ISO tests now run against their own postgres database **`am7isotestdb`** (`jdbc:postgresql://localhost:15430/am7isotestdb`, same creds as Objects7's `am7db`, separate database), set in this module's `resource.properties`. ISO owns **every** org from a blank DB, so `DEFAULT_FILE_BASE` stays module-local `./am7` and all keystores/vaults/streams are generated locally on first run. **Result: the cross-module keystore log noise is gone** — verified zero `Key store does not exist` / "Organization already exists" lines. The stale module-local `am7/` from the prior `am7db` runs (org-14 keystore) was moved aside (`am7.bak-old-am7db-*`) so the fresh DB regenerated a clean `./am7` (orgs 1–4).

**Correction (I was wrong, 2026-06-22):** I earlier concluded option #1 (Agent7's `DEFAULT_FILE_BASE=../AccountManagerObjects7/am7`) *fundamentally* could not work for ISO. That was incorrect. While ISO shared Objects7's `am7db`, option #1 works **by design** — the org records and their keystores stay consistent as long as the base path in effect at org-creation matches. The failure I observed was a **stateful ordering artifact of my own making**: I had already created the `/ISO42001` org's keystore under `./am7` in an earlier run, so repointing to Objects7's `am7` mid-stream couldn't find it. Had the base been Objects7's `am7` from the org's first creation, it would have been written there and worked. Now moot — the dedicated DB makes `./am7` the right base — but the original "rejected option #1" reasoning was a mistake, not a property of the design.

**Judgment calls flagged (⚠):**
- Mapper placed in `…iso42001.util` (not `engine`) to keep the `engine` package free of Objects7 model/IO imports — `NameBankLoader` already lives in `util`. Either location was sanctioned by the prompt.
- `DEFAULT_FILE_BASE` is `./am7` because ISO now owns a dedicated DB (`am7isotestdb`) whose orgs' keystores are module-local. (This is *not* because option #1 was unworkable — see correction above; it is simply the right base for the dedicated-DB design.)

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS; `mvn -o test -Dtest=TestISO42001AnalysisProfile,TestISO42001Models` against `am7isotestdb` → **BUILD SUCCESS — Tests run: 9, Failures: 0, Errors: 0** (4 analysisProfile + 5 Phase-1 models on the shared `ISO42001BaseTest`), **clean log (no keystore/org-exists noise)**. First live-DB exercise of the analysisProfile model + the new `testConfig.analysisProfile` column, and first run on the dedicated ISO database.

**Judgment calls flagged (⚠) for review:**
- **Fisher's exact & Kruskal-Wallis are not built into Commons Math 3.6.1** — implemented on its primitives: Fisher via `HypergeometricDistribution` (sum of tables with P ≤ P(observed)); KW via `NaturalRanking` + `ChiSquaredDistribution` (tie-corrected H, df=k-1). Math is standard/fully-determined; not a spec invention.
- **Odds-ratio → verdict — RESOLVED (2026-06-22) via `ScoringConfig`.** §4.4 leaves OR thresholds undefined (OR centers at 1, not 0). Now: OR is folded to a symmetric magnitude `max(OR, 1/OR)` and compared to configurable thresholds (defaults small=1.5 / medium=2.5). Tunable per campaign/rule; verified by `testOddsRatioVerdict`. The default values themselves remain open for Stephen to set authoritatively.
- **Score normalization formula** unspecified — min-max `(v-min)/(max-min)` clamped; scale now configurable (default 1–10) via `ScoringConfig`.
- **Lexicon file naming** — design §6.1 sketches 3 files (authority/stereotype/dignity); the only authoritative word lists are bias §2.3's 7 categories, so 7 verbatim files were shipped and loaded generically (the 3 families map onto them; see `LexicalAnalyzer` javadoc).
- **Name-bank content** — bias §2.1 gives 3 seed names/cell with "…"; extended to the spec-required ≥10 with signal-bearing, non-caricature names.

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS; `mvn -o test -Dtest=<5 Phase-2 classes>` → BUILD SUCCESS, **Tests run: 37, Failures: 0, Errors: 0** (33 core + 4 config: configurable effect thresholds, configurable α, odds-ratio verdict, configurable normalization scale).

### Phase 3 — Test Execution Engine + full bias suite (2026-06-22)
**Scope:** the runner that executes bias probes end-to-end through the AM7 chat infrastructure, plus all 7 bias module executors, persisting reproducible, logged, verdicted results. Built on the Phase-2 engine (`StatisticalAnalyzer`, `BiasScorer`, `LexicalAnalyzer`, `NameBankLoader`/`name_banks.yaml`, `ScoringConfig`) and the Phase-3a `util.ScoringConfigMapper.resolve(testConfig)`. B1 (multi-provider/tool-call) held off; cross-model aggregation/heat-map deferred to Phase 5; BIAS-VIS (image) out of scope.

**Delivered (all in `org.cote.accountmanager.iso42001.*`, no Objects7 changes):**
- `engine.TrialSpec` / `engine.TrialPlan` — a fully-determined, **seeded, interleaved** schedule of trials. `TrialPlan.build(module, bank, perGroup, tier, seed)` draws subjects and the round-robin group order from one `Random(seed)`, so same seed ⇒ identical plan (`equals`/`signature`). This is the reproducibility property the tests assert (never on LLM text).
- `engine.AnalysisKind` (NUMERIC/BINARY) + `engine.BiasModule` (abstract) — each module supplies its comparison groups + per-group subject tokens, the verbatim Tier-1 system/user prompt, the Tier-2 conversation turns, and a `observe(...)` reducer. Prompt text transcribed verbatim from `iso42001-bias.md`; no bias content invented.
- 7 module executors in `engine.modules`: `AttrModule` (BIAS-ATTR-002), `HireModule` (BIAS-HIRE-001), `RefusalModule` (BIAS-REF-001), `AssocModule` (BIAS-ASSOC-002), `HealthModule` (BIAS-HC-001), `LoanModule` (BIAS-FIN-001), `NarrModule` (BIAS-NARR; variant-selectable EDU-003 / HR-001 / RE-001, default rec-letter).
- `engine.TestExecutor` (base) + `engine.BiasTestExecutor` — isolated session per trial via `olio.llm.Chat` against the configured endpoint; Tier-1 (system+user) and Tier-2 (multi-turn, assistant replies appended); keyframe/memory/prune/interaction machinery neutralized so a trial is exactly the prompt under test. Wires name banks → prompts → chat → `BiasScorer`/`LexicalAnalyzer` → per-group aggregation → `StatisticalAnalyzer.classifyVerdict(p, effect, type, cfg)` under the resolved `ScoringConfig` → one `iso42001.testResult`. **Verbatim** request/response captured per call.
- `engine.TestRunner` — orchestrates from `iso42001.testConfig`: resolves the `ScoringConfig`, builds the plan, creates the `iso42001.testRun` (PENDING in-memory → persisted RUNNING → updated COMPLETED/FAILED with embedded `results`), records `randomSeedUsed`, rolls up `passCount`/`flagCount`/`failCount`/`totalTrials` (= LLM calls), writes the verbatim raw log to a `data.data` (in the run user's home) and sets `rawLogRef`.

**Tests (live LLM, `@Category(LiveTest.class)`, all as non-admin `isoTester` against `/ISO42001`):** `BiasModuleTestBase` (shared: ollama config via `OlioTestUtil.getOllamaOpenAIConfig` → DGX Spark, name-bank load, reproducible-plan + persistence + verbatim-log + verdict assertions, graceful endpoint-down degradation) + per-module `TestISO42001BiasAttr/Hire/Refusal/Narr/Assoc/Health/Loan`. Each asserts: seeded plan reproducible (same seed ⇒ equal plan), `testRun`+`testResult` re-readable, `randomSeedUsed` matches, `rawLogRef`→`data.data` carries verbatim prompt text, a verdict assigned via the resolved `ScoringConfig`. `TestISO42001BiasAttr` also exercises `ScoringConfigMapper.resolve` with a real `analysisProfile`. `TestISO42001BiasRefusal` carries the negative-RBAC assertion (isoReader create of a `testRun` denied — AUDIT path confirmed). New `LiveTest` category marker added so the live suite is selectable/excludable from the pure-logic `UnitTest` suite.

**Judgment calls flagged (⚠):**
- **BIAS-NARR was authored into existence (2026-06-23).** It had no module in `iso42001-bias.md` (only design §5.1's conceptual mapping) and was part of *our own* design taxonomy (§11/§5.1), so per Stephen ("you proposed those, so if they don't exist we need to find or make them") it is now a **first-class module**: a new **§3.12 BIAS-NARR** spec was written into `iso42001-bias.md`, and `NarrModule`'s default variant is **BIAS-NARR-001** — a character-portrait **swap test** (design §5.2) scored by a deterministic *favorability composite* (dignity + agency + physical-detail specificity − negative-framing − antagonist-role markers; two NARR-only lexicons `physical_detail`/`antagonist` added without disturbing the shared 7-category set, via a new `LexicalAnalyzer.fromClasspath(String...)` overload). It directly operationalizes the CLAUDE.md overcorrection areas (1,2,3,4,5,7,8,9,10). The earlier stopgap (aliasing BIAS-EDU-003) is gone; EDU-003 / HR-001 / RE-001 remain as selectable `NarrModule.Variant`s so a user/group can pick. Religion/political swap variants (design §5.2 dims) are documented as the near-term extension.
- **BIAS-ASSOC** runs **ASSOC-002** (sentence-completion valence), not ASSOC-001: ASSOC-001's scoring needs a BLS profession→income-tier table that `iso42001-bias.md` does not provide (underspecified). Valence is scored via the spec-defined `threat_language` lexical category (§2.3) as a deterministic substitute for VADER sentiment (this project ships no sentiment model). Uses only spec-provided word lists.
- **BIAS-ATTR** runs **ATTR-002** (controlled identical-profile, name-varied) rather than ATTR-001 (model self-assigns race) — the cleaner protected-class comparison.
- **Refusal/Assoc/Narr Tier-1 send no system prompt** (the spec defines none for those); the chat layer's default roleplay system prompt is explicitly cleared so Tier-1 is bare.
- **`tier=0` (both)** currently executes Tier 1 only; running both tiers per run is a later extension.
- **Test N is small** (`samplesPerGroup=2`, Tier 1) to bound runtime on the single-threaded DGX Spark ollama; degenerate 2×2 tables / insufficient-N MWU are handled as no-difference → PASS (and noted on the result). With identical qualifications the model frequently hires/approves both groups, yielding a non-significant (PASS) verdict — expected, and the engine records the degeneracy in `notes`.
- **Endpoint-down handling:** if every trial's call fails, the result is verdict `ERROR` with a "unreachable" note (no fabricated stats); the seeded-plan, persistence, logging, and RBAC paths still run and assert.

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS; `mvn -o test -Dtest='TestISO42001Bias*'` against `am7isotestdb` with Spark (192.168.1.42) live → **Tests run: 25, Failures: 0, Errors: 0** (7 live module classes = 15 tests, all PASS verdicts this run; + 10 `TestISO42001BiasScorer` unit). Critical set (ATTR/HIRE/REFUSAL) green; NARR/ASSOC/HEALTH/LOAN green. All operations as non-admin role users against `/ISO42001`; negative RBAC confirmed.

*(Add per-phase detail blocks for 4–10 as each is started, copying the gate from the tracker and expanding scope/tests/DoD from the prompt template.)*

---

## Progress log

| Date | Phase | Change | Result |
|---|---|---|---|
| 2026-06-19 | — | Plan + prompt template captured; tracker created | n/a |
| 2026-06-19 | 1 | Pinned Maven coords (`org.cote.accountmanager:AccountManagerISO42001:7.0.0-SNAPSHOT`); confirmed Objects7 test-jar available | Ready |
| 2026-06-19 | 1 | Spot-checked design §2: all 7 model specs complete; base `inherits` all exist. Reconciled 4 inconsistencies — role names → CamelCase 6-role set (incl. Auditors), cert model → `iso42001.certificationRequest`, added `access.roles` to testConfig/testRun, documented embedded vs first-class model test approach | §2 build-ready |
| 2026-06-21 | 1 | Built `AccountManagerISO42001` (pom + parent module), 7 model JSONs, `ISO42001ModelNames`, 6 ISO roles, `TestISO42001Models`. All 5 first-class models group-backed (Stephen's call); fixed `expiryDate` dup (inherited), dropped `certificationRequest` `dedicatedParticipation` (participation-name collision), added field-level FK RBAC. `mvn install` Objects7 → `mvn test` ISO42001 | **BUILD SUCCESS — Tests run: 5, Failures: 0, Errors: 0; 5/5 negative-RBAC denials verified** |
| 2026-06-22 | 2 | Statistical engine & scoring: added commons-math3 3.6.1 + snakeyaml 2.3 to ISO pom; `StatisticalAnalyzer` (MWU/Chi-sq/Fisher/KW/Bonferroni/Cohen's d/odds ratio/Cramér's V/verdict), `BiasScorer`, `LexicalAnalyzer` + 7 lexicon files, `NameBankLoader` + `name_banks.yaml`, `SwapTestRunner`. Fisher/KW implemented on Commons Math primitives (not built-in); flagged OR-verdict, normalization, lexicon-naming, name-bank-content judgment calls. `mvn -o test -Dtest=<5 Phase-2 classes>` | **BUILD SUCCESS — Tests run: 33, Failures: 0, Errors: 0** (all vs hand-computed fixtures) |
| 2026-06-22 | 2 | Externalized all thresholds/formulas into `engine.ScoringConfig` (α, Bonferroni toggle, Cohen's-d/Cramér's-V + odds-ratio thresholds, normalization scale) per Stephen — campaign-wide default with per-rule override; resolves the OR-verdict gap (fold to max(OR,1/OR) vs tunable thresholds). Wired into `classifyVerdict`/`BiasScorer`/`SwapTestRunner`; added 4 config tests. Persistence model (extend `testConfig` vs new `analysisProfile`) deferred to Phase 3 — decision needed. `mvn -o test -Dtest=<5 Phase-2 classes>` | **BUILD SUCCESS — Tests run: 37, Failures: 0, Errors: 0** |
| 2026-06-22 | 2→3 | Persistence decision made (Stephen): dedicated `iso42001.analysisProfile` model. Added `analysisProfileModel.json` (8 ScoringConfig fields, group-backed, testConfig RBAC), registered in `ISO42001ModelNames`, added `testConfig.analysisProfile` foreign ref. Both JSONs validated; compile + pure-logic tests green. **Model NOT yet DB-CRUD-verified — deferred to Phase 3.** No DB-level change made (schema auto-emits on first open). `python json.load` + `mvn -o test` | **JSON OK; BUILD SUCCESS — Tests run: 37, Failures: 0, Errors: 0** |
| 2026-06-22 | 3a | Analysis Profile persistence & ScoringConfig binding. Added `util.ScoringConfigMapper` (`fromRecord`/`toRecord`/`resolve` — campaign-wide default, per-rule override left as documented hook) + `TestISO42001AnalysisProfile` (Integration, live DB, all non-admin): analysisProfile CRUD + negative RBAC, `testConfig.analysisProfile` foreign round-trip, `fromRecord` stored-vs-default fidelity, resolver set/null. First live-DB exercise of the new model + new `testConfig.analysisProfile` column (schema auto-emitted on open; no DB change). `mvn -o test -Dtest=TestISO42001AnalysisProfile` | **BUILD SUCCESS — Tests run: 4, Failures: 0, Errors: 0** |
| 2026-06-22 | 3a | Extracted `ISO42001BaseTest` (shared scaffolding; both ISO integration tests now extend it). Kept `DEFAULT_FILE_BASE=./am7`. ⚠ I tried Agent7's `../AccountManagerObjects7/am7` mid-stream and saw all ISO tests fail, then **wrongly concluded option #1 was fundamentally incompatible** — it actually failed only because the `/ISO42001` org's keystore had already been created under `./am7` in a prior run (a stateful artifact); option #1 works by design on a shared DB. Corrected below. `mvn -o test -Dtest=TestISO42001AnalysisProfile,TestISO42001Models` | **BUILD SUCCESS — Tests run: 9, Failures: 0, Errors: 0** |
| 2026-06-22 | 3a | Option #2 adopted: Stephen provisioned a dedicated blank DB `am7isotestdb`; pointed ISO `resource.properties` `test.db.url` at it (same creds), kept `DEFAULT_FILE_BASE=./am7`, moved the stale `am7/` (old am7db org-14 keystore) aside so first run regenerated a clean local `./am7` (orgs 1–4). ISO now owns every org → **cross-module keystore log noise eliminated (0 lines)**. `mvn -o test -Dtest=TestISO42001AnalysisProfile,TestISO42001Models` | **BUILD SUCCESS — Tests run: 9, Failures: 0, Errors: 0; clean log** |
| 2026-06-22 | 3/4 | Planning: B1 (multi-provider/tool-call) held off. Phase 4 (bias module) **merged into Phase 3** (Stephen, option B) — build the execution engine and the full 7-module bias suite together, run live against the ollama deployment (mix of models) / optional Azure OpenAI models. Reconciles design §11 (which already lists all bias classes under Phase 3) with the tracker. | Plan updated; Phase 3 prompt revised |
| 2026-06-23 | 3 | **BIAS-NARR made a real module** (was a design-doc-only concept; Stephen: "you proposed those … find or make them"). Authored `iso42001-bias.md` §3.12 BIAS-NARR spec; reworked `NarrModule` default → **BIAS-NARR-001** character-portrait swap test (favorability composite scoring); added `physical_detail`/`antagonist` lexicons + `LexicalAnalyzer.fromClasspath(String...)` overload (shared 7-cat set unchanged — Phase-2 lexical test still 4/0/0). EDU/HR/RE kept as selectable variants. `mvn -o test -Dtest='TestISO42001Bias*'` + `TestISO42001LexicalAnalyzer` | **BUILD SUCCESS — Tests run: 25, Failures: 0, Errors: 0**; NARR-001 ran live (composite Cohen's d), REF-001 returned FAIL this run (correct differential-refusal semantics; verdict-assigned assertion holds) |
| 2026-06-22 | 3 | **Execution engine + full bias suite.** Added `engine.TrialSpec`/`TrialPlan` (seeded interleaving), `AnalysisKind`, `BiasModule` + 7 module executors (`engine.modules`), `TestExecutor`/`BiasTestExecutor` (live `Chat` integration, verbatim capture, scoring→verdict), `TestRunner` (testRun lifecycle, rollup, rawLog `data.data`→`rawLogRef`). 7 live `@Category(LiveTest.class)` test classes + `BiasModuleTestBase` + `LiveTest` marker, all as non-admin isoTester. ⚠ BIAS-NARR→BIAS-EDU-003 (no NARR module in bias.md; Stephen's call; HR/RE variants selectable); BIAS-ASSOC→ASSOC-002 w/ `threat_language` lexical valence (ASSOC-001 BLS table undefined); ATTR-002 (controlled); null-system tiers cleared of default roleplay prompt; N=2 for runtime; endpoint-down → ERROR (no fabricated stats). `mvn -o test -Dtest='TestISO42001Bias*'` vs am7isotestdb, Spark 192.168.1.42 live | **BUILD SUCCESS — Tests run: 25, Failures: 0, Errors: 0** (15 live: ATTR/HIRE/REFUSAL/NARR/ASSOC/HEALTH/LOAN all PASS verdicts + 3 reproducible-plan + neg-RBAC; 10 scorer unit). Embedded results persist on update; rawLogRef verbatim verified; seed reproducible |

> Append one row per working session: what was attempted, the test command run, and the actual outcome (BUILD SUCCESS/failure, counts). Keep it honest — failures stay logged, not erased.
