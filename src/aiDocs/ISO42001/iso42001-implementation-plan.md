# ISO 42001 — Implementation Plan & Progress Tracker

**Purpose:** The living tracker for building the ISO 42001 subsystem + the enterprise-readiness work. Update the **Status** and **Progress log** as phases complete. Authoritative design is [`iso42001-design.md`](iso42001-design.md); requirements in [`iso42001.md`](iso42001.md); enterprise context in [`enterpriseReadiness.md`](enterpriseReadiness.md). Start each phase from [`iso42001-prompt-template.md`](iso42001-prompt-template.md).

**Last updated:** 2026-06-24 (Phase 7 ✅ REST/MCP shim + ComplianceService; Phase 8 ✅ Ux752 views + `compliance` profile + 8 models into `modelDef.js` + self-service ISO test-user provisioning; live e2e green incl. positive ISO-Tester create over REST. Also: Service7 vector-support startup made config-governed so an embedding-server outage no longer disables vector DB. Next: Phase 9 Docker appliance, Phase 10 integration.)

> **End-user UAT in progress (Stephen, 2026-06-24).** Stephen is manually testing the Phase 7/8 deliverables. Test script: [`iso42001-enduser-tests.md`](iso42001-enduser-tests.md) (A–H: feature/profile, dashboard, test runner, results, reports, certification, RBAC, MCP). Issues found there will be triaged + fixed before Phase 8 is considered field-validated. Test accounts (live `/Development`): `e2etest_iso42001`/`password` (all ISO roles) and `e2etest_shared`/`password` (no ISO role).

Status legend: ⬜ Not started · 🟡 In progress · ✅ Done · ⏸ Blocked

---

## Standing constraints (apply to EVERY phase)

These are non-negotiable and are baked into the prompt template. A phase is not "done" if any are violated.

1. **Integrated system tests, not mocks.** All services are live. "Tested" = real CRUD/flows exercised through `AccessPoint` / live service layer against the real DB. No `typeof`/`.parse()`/skipped tests. The Objects7 `BaseTest` pattern already provides this — reuse it.
2. **Test organization: `/ISO42001`.** All backend tests run against this org (`OrganizationEnumType.DEVELOPMENT`), created if absent.
3. **Admin creates users only.** Use the org Admin user ONLY to create test users and assign roles. **Never run a test assertion as Admin.** Every operation under test runs as a non-admin role user.
4. **DB reset policy.** The test `resource.properties` may set `test.db.reset=true` to rebuild the test schema (one-shot, BaseTest auto-clears it). That is the ONLY sanctioned reset path. **Never** drop/recreate the docker postgres container; **never** directly `TRUNCATE`/`DROP`/`DELETE` tables, schema, or data. Committed default: `test.db.reset=false`.
5. **Frontend tests** use `ensureSharedTestUser()` (Ux752 `e2e/helpers/api.js`) — never admin.
6. **Honesty.** Report exactly what passed and what could not be tested. No "tested"/"working" claims without a green run shown. Flag judgment calls (⚠) and ask before deviating from the spec.

---

## Phase tracker

| # | Phase | Status | Gate (definition of done) | Design ref |
|---|---|---|---|---|
| 1 | Foundation — module, 7 models, RBAC roles, `/ISO42001` test harness | ✅ | `mvn -pl iso42001 test` green: `TestISO42001Models` CRUD + RBAC, all as non-admin users | §2, §6.2, §6.3, §7, §9A.1 |
| 2 | Statistical engine & scoring | ✅ | Tests vs hand-checked fixtures: Mann-Whitney U, Chi-square, Fisher, Kruskal-Wallis, Cohen's d, odds ratio, Cramér's V, verdict logic | §4 (`iso42001.md`), §12.2 |
| 3 | Execution engine + **full bias suite** (Tier-1/2, seeded interleaving, verbatim logging; all 7 bias modules) | ✅ | Live runs vs configured endpoint(s) produce reproducible, logged, persisted results; each bias module scores + verdicts a protected-class comparison end-to-end | §11 Phase 3, [`iso42001-bias.md`](iso42001-bias.md) |
| ~~4~~ | **Merged into Phase 3** (2026-06-22, Stephen chose option B: build the engine and the full bias module set together, run live against the ollama deployment with a mix of models / optional Azure OpenAI models). Was: "Bias module (start: ATTR/HIRE/REF critical set)". | — | — | — |
| 5 | Reporting & signed PDF export | ✅ | Result set → report JSON (rollups/verdict/sections/hash) → PDF (`%PDF`, charts, NOT-CERTIFIED block) stored as `data.data`; `mvn -o test -Dtest=TestISO42001Report,TestISO42001PdfExport` green, all non-admin | §4, §12.3 |
| 6 | Certification (request/approve/sign/verify/revoke + message thread) | ✅ | Full lifecycle; signature verifies; revoke invalidates | §3, §2.7–2.8 |
| 7 | REST & MCP **thin shim** (`ISO42001Service` + `ComplianceService`) — marshals HTTP/MCP ↔ the already-tested Phase 3/5/6 service methods; **no new business logic** | ✅ | Shim + MCP providers + startup wiring built; Service7 compiles + war packages with ISO jar. Component test `TestISO42001Service` (hand-rolled mock, live `am7isotestdb`) 4/0/0. **Live Tomcat:** `e2e/iso42001.spec.js` (Playwright, non-admin `ensureSharedTestUser`) **3/3 green** — /modules reachable+serializes, boundary RBAC enforced (non-ISO-role create → 403), MCP JSON-RPC round-trip (`tools/list` exposes `iso42001_*` + `am7_`). Positive ISO-role create→report→certify over HTTP deferred to Phase 8 (needs a role-assignment e2e helper); covered at DB/component level now. | §6.4, §1.3–1.4, [`../ISO42001Plan.md`](../ISO42001Plan.md) |
| 8 | Ux752 feature views + `compliance` profile | ✅ | **Built:** `compliance` profile (§9.2); ISO context roles in `router.js setContextRoles` (§9A.1); `features/iso42001/` = 5 views (dashboard/testRunner/resultsBrowser/reportViewer/certificationView) + `routes.js` + feature-local `iso42001Client` + RBAC helpers; `featureConfig` Compliance button; 8 ISO models spliced into `modelDef.js` from live `/rest/schema`; self-service `ensureIso42001TestUser` e2e helper (creates user + assigns ISO roles via the authZ member endpoint). **Verified:** Vitest 7/7 (incl. model presence), `vite build` ✓, live e2e **8/8** — dashboard render (non-admin), REST reachable+serialize, negative RBAC (non-role create→403), MCP round-trip, **provisioning assigns ISO Tester role**, **ISO Tester create+read testConfig over REST (200)**. ⚠ Deferred: browser-driven run→report→certify interaction (needs live data/LLM — Phase 10) + simplified charts/heat-map; formDef §9A.2 skipped (hand-rolled views) | [`iso42001.md`](iso42001.md) §9, design §9A |
| 9 | Docker Compose template (`deploy/` + `generate.sh`) | ⬜ | Clean host → `make generate && make up` → working compliance appliance | [`enterpriseReadiness.md`](enterpriseReadiness.md) |
| 10 | Integration & validation | ⬜ | Full run → report → certify → export vs live endpoints; audit-artifact review | §11 Phase 9 |

### Execution grouping — backend-first, then one appserver+frontend block (2026-06-23, Stephen)

To avoid bouncing Tomcat repeatedly, the remaining phases are split into two **sequential tracks**. Finish all of the backend track (no appserver, no browser) before starting the appserver/frontend track, which is then done in a single Tomcat+Ux session.

| Track | Phases | Needs | How tested |
|---|---|---|---|
| **A — Backend (no appserver)** | **5** Reporting & PDF → **6** Certification | nothing new — JUnit + live service layer vs `am7isotestdb` | `cd AccountManagerISO42001 && mvn -o test` (same harness as Phases 1–3) |
| **B — Appserver + Frontend** | **7** REST & MCP (`ISO42001Service` in Service7) → **8** Ux752 views + `compliance` profile | **Tomcat** (Service7 webapp) **+ Ux** (Vite dev server) | live HTTP/MCP round-trips + Vitest/Playwright (`ensureSharedTestUser()`) |

- **Do Track A next** (Phase 5 first). It produces reports + signed PDFs from the test runs the engine now generates — pure backend, verified exactly like Phases 1–3. **No Tomcat or Ux needed for Track A.**
- **Backend-complete-before-shim (locked, 2026-06-23, Stephen).** Every backend capability MUST be exposed as a **service/engine-callable method** (takes the context user + ids/records, returns records/bytes) and **fully unit-tested against live backend services** by the end of Track A — this already holds for the Phase-3 engine (`TestRunner`/`BiasTestExecutor`) and is the bar for Phase 5 (`ReportGenerator`/`PdfExporter`) and Phase 6 (`ISO42001CertificationFactory` create/verify/revoke). Consequence: **Phase 7 is a thin transport shim** — `ISO42001Service` (REST) + the MCP tools/resources only marshal HTTP/JSON-RPC ↔ those already-tested methods, apply auth, and serialize. No business logic lives in the shim, so Phase 7's tests cover only wiring/auth/serialization (endpoint reachable, RBAC enforced at the boundary, request/response round-trips), not re-testing logic. **All remaining substantive effort is Phase 8 (Ux implementation + testing).**
- **Track B starts the appserver once.** Phase 7 (REST/MCP shim) and Phase 8 (Ux752) are executed back-to-back in one session so Tomcat is started/bounced minimally and the Ux dev server runs alongside. Stephen starts Tomcat + Ux at the top of Track B, not before.
- The numbered phase order in the tracker is unchanged; this is the **execution sequencing** (A before B), making the front/back split explicit.

### Parallel enterprise tracks (do not block the ISO core)

| Track | Status | Gate | Ref |
|---|---|---|---|
| E1 | SSO federation completion (splice `TokenFilter`/`LoginService` TOKEN stub + JWKS trust + JIT provisioning) | ⬜ | OIDC login against a live IdP issues an AM7 session; JIT-provisioned user | `enterpriseReadiness.md` gap #1 |
| E2 | SCIM completion (deprovisioning semantics, enterprise ext, Roles/Entitlements, conformance) | ⬜ | Live IdP create→update→group→disable→re-enable→delete, all org-scoped + audited | `enterpriseReadiness.md` SCIM plan |
| E3 | GA version pinning + dependency scanning | ⬜ | Build on GA Java/Jakarta; scan in place | `enterpriseReadiness.md` P0 |
| E4 | OpenAPI + API versioning + rate limiting; metrics/health | ⬜ | Spec generated; `/health` live; limiter enforced | `enterpriseReadiness.md` P1 |

---

## Backlog — captured requirements (2026-06-22, Stephen)

New requirements to fold into the design + phasing. Not yet scheduled into a phase; tracked here so they aren't lost.

| # | Requirement | Status | Gate (definition of done) | Lands in |
|---|---|---|---|---|
| B1 | **Multi-provider chat + tool flow.** The chat config currently works against OpenAI-shaped APIs (only Azure OpenAI has actually been tested) and does **not** handle the tool/function-call flow. Extend it to cover the top 5–10 AI API types (~80% of usage) — candidates: OpenAI, Azure OpenAI, Anthropic, Google Gemini, AWS Bedrock, Mistral, Cohere, plus local Ollama/vLLM — including each provider's tool-call request/response loop. | ⬜ | A live call + a multi-step tool-call round-trip succeeds against ≥5 provider types via the chat config; provider-specific request/response + tool schemas adapted; verbatim logging preserved | Prereq/extension of **Phase 3** (execution engine runs tests through the chat layer); design §1.2 |
| B2 | **AI-policy ISO-compliance check.** A feature that evaluates an organization's AI policy document(s) against ISO 42001 control requirements and reports conformance/gaps. **Awaiting Stephen's example policy set** to anchor the rubric and fixtures. | ⬜ (blocked: example set) | Given the sample policy set, the feature maps each policy clause to ISO 42001 Annex A controls and produces a per-control conformance verdict + gap list; verified against a hand-graded fixture | New module alongside the bias suite; ties into reporting (**Phase 5**) + MCP/REST (**Phase 7**) |
| B3 | **US Federal + State regulatory check.** A feature that checks AI policies/practices against published US Federal and specific State AI regulations (only where published/available). | ⬜ | A regulation catalog (Federal + named States) is loaded; a policy is evaluated against applicable regs with citations; verified against a hand-graded fixture; clearly scoped to *published* regs only | Sibling of B2; shares the policy-evaluation + reporting infrastructure |
| B4 | **SCM repo AI-usage scanner.** Connect to a source-control repo (Git first, pluggable SCM) and scan for uses of AI — SDK/library usage, model references, prompt references, AI API endpoints — to build the ISO 42001 AI-system **inventory** (A.5.4 impact assessment, Clause 6 risk). Design §13. | ⬜ | Given a repo (clone or working tree), produces an inventory of findings (file/line/category/signature/provider/snippet); signature-matcher core verified against fixture files with hand-checked findings; org-scoped + RBAC-gated; feeds report inventory/appendix | New `…iso42001.scan` module + `iso42001.repoScan`/`aiUsageFinding` models; JGit dependency (Phase-gated — ask before adding). Design §13 |
| B5 | **External signer/root cert at org/system setup.** Org root keys are currently **self-signed** (`CertificateUtil.generate` with `signer==null` ⇒ self-signed CA); there is no path to supply/import an external signer (or root/intermediate CA) when creating a new org or doing first-time system setup. Surfaced scouting Phase 6 (Stephen, 2026-06-24). | ⬜ | New org / first-run setup can optionally import a signer or root cert so the org keystore chains to an external CA instead of being self-signed; existing self-signed default preserved when none supplied | `CertificateUtil.generate`/`KeyStoreUtil.getCreateStore` + org-creation / setup path (Objects7 crypto infra). Does NOT block Phase 6 — ISO certifier certs chain to the (self-signed) org root for now |
| B6 | **Certificate revocation lists (CRLs).** X.509 CRLs are not considered anywhere in the crypto/cert infrastructure. Phase-6 certification revocation is **application-level** (`iso42001.certification.status=REVOKED`, checked by `verifyCertification`), not X.509 CRL distribution/checking. Surfaced scouting Phase 6 (Stephen, 2026-06-24). | ⬜ | CRL generation + checking integrated into cert verification (issue/distribute CRL, verify consults it); ISO `verifyCertification` additionally honors CRL state | Objects7 crypto infra (`CertificateUtil`/`KeyStoreUtil`) + ISO `CertificationVerifier`. Does NOT block Phase 6 — record-status revocation is sufficient for the Phase-6 gate |
| B7 | **Round out + unit-test the cert/keystore services.** `CertificateUtil`/`KeyStoreUtil` were ported from AM6.5 (XSD models); many of the AM6.5 certificate **services and tests were not brought over**. The code works (AM7 startup + Vault depend on it) but is obtuse, likely missing convenience helpers (e.g. load/fetch an existing keystore/cert without regenerating, (de)serialize `crypto.signature`, verify a cert chain), and has **no dedicated cert unit tests** — coverage is only the implicit "AM7 must start and Vault must work." Surfaced scouting Phase 6 (Stephen, 2026-06-24). | ⬜ | Direct unit tests for `CertificateUtil`/`KeyStoreUtil` (generate self-signed + signed, sign/verify a payload, encode/decode DER, validity/expiry, P12 round-trip); missing helpers added with coverage; API cleaned up where obtuse | Objects7 crypto infra. Does NOT block Phase 6 — but Phase 6's `ISO42001CertificationFactory` tests become the **first real exercise** of signed-cert creation + verify, so capture any missing helper or rough edge here and either add it to Objects7 (with a test, per Objects7 `CLAUDE.md`) or note it for B7 |

**Notes / open questions:**
- **B1** is effectively a precondition for running Phase-3 live tests against anything beyond Azure OpenAI; recommend pulling it into Phase 3 scope. The tool-call flow is net-new (current config has none).
- **B2** is **blocked pending the example policy set** Stephen will provide — the rubric, control mapping, and test fixtures should be derived from that set rather than invented (per the no-invented-specs rule).
- **B2/B3** introduce a *document/policy compliance* capability distinct from the *runtime bias-testing* capability that Phases 2–4 build. They likely warrant their own design section in `iso42001-design.md` and their own model(s) (e.g. `iso42001.policy`, `iso42001.regulation`, `iso42001.conformanceResult`). Flagging as an architecture decision before implementation.
- **B4** is a *code-inventory* capability (scan repos for AI usage) — a third distinct family alongside runtime bias-testing and document/policy compliance. Design §13 captures detection categories, the JGit-based architecture, and suggested `iso42001.repoScan` / `iso42001.aiUsageFinding` models. The signature-matcher core is pure-logic + fixture-testable (Phase-2 style); only the repo traversal needs a live clone. JGit dep + new tables are **not** added until B4 is scheduled (ask first).
- **B5/B7 — AM6.5 reference (capability checklist, NOT a port target).** The previous AM6.5 PKI tooling lives in the adjacent repo: `C:\Projects\GitHub\AccountManager\src\AccountManagerUtil\.../util/OpenSSLUtil.java`, its CLI wrapper `.../console/OpenSSLAction.java`, and the end-to-end test `.../util/TestOpenSSLUtil.java`. It is **external-process/file-based** (shells out to the `openssl` binary over a `certificates/`+`keys/`+`stores/` tree): generate root CA, generate CSR, CA-sign a CSR, amend cert chain, export PKCS12 (public + private-with-chain), import P12/cert into JKS key/trust stores, extract key/cert, encrypt/decrypt round-trip. AM7 deliberately replaced this with **in-JVM BouncyCastle + model/DB-backed** `CertificateUtil`/`KeyStoreUtil`, so **do not port it verbatim** — it would regress the architecture. Use it as the behavioral spec for what B5 (root/CA + chain + external-signer import) and B7 (the missing end-to-end create→sign→chain→export→import→use test + convenience helpers) should ultimately cover. Stephen confirms a little more is implemented in AM6.5 than in AM7, still old-style and likely with its own gaps.

---

## Per-phase detail

### Phase 1 — Foundation
**Architecture (locked):** All ISO 42001 code + models live in their **own** Java/Maven project under packages `org.cote.accountmanager.iso42001.*`. Service7 adds it as a **dependency**; Objects7 is NOT modified. Models register at runtime via `ISO42001ModelNames extends ModelNames` + `use()` (the `OlioModelNames` pattern — design §6.3, §12.1).

**Pinned Maven coordinates (locked 2026-06-19):**

| Field | Value |
|---|---|
| `groupId` | `org.cote.accountmanager` |
| `artifactId` | `AccountManagerISO42001` |
| `version` | `7.0.0-SNAPSHOT` |
| `packaging` | `jar` |
| Project dir | `AccountManagerISO42001/` (sibling of `AccountManagerObjects7`) |

Service7 `<dependency>` uses the same `groupId`/`artifactId`/`version`. (`-pl` short name in build commands is normalized by a project script.)

**Scope:** new project (depends on Objects7 + its `test-jar`); 7 models per design §2 under `src/main/resources/models/iso42001/`; `ISO42001ModelNames` registration class; call `ISO42001ModelNames.use()` in test setup; 6 ISO roles (CamelCase): `ISO42001Testers`, `ISO42001Reporters`, `ISO42001Certifiers`, `ISO42001Readers`, `ISO42001Auditors`, `ISO42001Administrators`.

**Model naming & shape (reconciled 2026-06-19):**
- First-class (group-backed, own `access.roles`): `testConfig`, `testRun`, `report`, `certification`, `certificationRequest` (note: full name `iso42001.certificationRequest`, file `certificationRequestModel.json`).
- Embedded list-element models (inherit `common.nameId` only; no group/constraints/access.roles — gated via their parent): `testResult` (inside `testRun.results`), `reportSection` (inside `report.sections`).
- §2 already carries `access.roles` on report/certification/certificationRequest; **add `access.roles` to testConfig + testRun** (Testers create/update; Readers/Auditors read; Administrators delete/admin).

**Tests:** `TestISO42001Models` — for each of the 5 first-class models: create→read→patch→list→delete via `AccessPoint` as an authorized non-admin role user, plus a negative RBAC assertion (unauthorized user denied). For the 2 embedded models: validate via their parent (a `testRun` round-trips its `results`; a `report` round-trips its `sections`). Admin used only to create the role users.
**DoD:** after `mvn -f AccountManagerObjects7/pom.xml install`, both `cd AccountManagerISO42001 && mvn compile` and `mvn test` show BUILD SUCCESS with the above passing.
**Test harness:** confirmed — Objects7 already publishes a `test-jar` (maven-jar-plugin 3.4.1); Agent7 consumes it. The ISO project's `test-jar` dependency reuses `BaseTest`/`OlioTestUtil` directly; no Objects7 change needed.

**Phase 1 outcome (2026-06-21) — DONE.** `AccountManagerISO42001` (`org.cote.accountmanager:AccountManagerISO42001:7.0.0-SNAPSHOT`, jar) created as a sibling of Objects7, added to the parent aggregator pom. 7 models under `src/main/resources/models/iso42001/`; `ISO42001ModelNames.use()` registers them (called in test setup before IO open, so `IOSystem.open` auto-creates the new tables via `generateNewSchemaOnly` — additive, no reset/drop). `TestISO42001Models` is green: 5 first-class models each do create→read(owner)→read(reader role)→patch→list→delete via `AccessPoint` as non-admin role users, plus a negative RBAC assertion (isoReader create denied — verified for all 5); the 2 embedded models round-trip through their parents (`testRun.results`, `report.sections`, 2 rows each). Admin user used only to create role users + assign roles + the shared group.

Decisions taken (all within the approved model-construction scope; flagged for review):
- **All 5 first-class models are group-backed** (Stephen's call, for hierarchical snapshotting): `certification` and `certificationRequest` gained `data.directory` beyond the §2 verbatim inherits.
- `certification` dropped its own `expiryDate` field — it is inherited from `common.dateTime` (the explicit redeclare collided on the DB column). The field is still present via inheritance.
- `certificationRequest` does **not** set `dedicatedParticipation` — so its entitlement/participation-association rows live in the **common (shared) participation table** rather than a dedicated per-model table. (With a dedicated table, `access.accessRequest`'s many `system.user`-typed participations produced a truncated participation-table-name collision.) `dedicatedParticipation` only controls where those association rows are stored — useful when a model's participation volume is large enough to want its own table/tablespace; it is not a property that inherits and dropping it loses no capability. `certificationRequest` is low-volume (≈one per certification), so the common table is the right fit. Group-backing (the `groupId` from `data.directory`) is independent and unaffected. The higher-volume models (`testRun`, `report`, plus `testConfig`/`certification`) keep their dedicated participation tables.
- **Field-level RBAC on `system.user` FKs** (`certification.certifier`, `certificationRequest.requestedCertifier`) — `create`+`read` ISO roles added so role members can reference users without the `AccountUsersReaders` system role (the `access.accessRequest`/`contactInformation` pattern). `report.exportedPdf` (→`data.data`) got field-level `read`.
- **Test query patterns** (no model/auth change): single-record reads use `find` by `objectId` (model read-role authorizes per record); list uses a `name`-constrained query so the coarse model-level read applies (a `groupId`-constrained list would instead force a group-read check on the admin-owned shared group, which the model read-role does not grant).

### Phase 2 — Statistical engine & scoring
**Scope (locked to design §11 Phase 2 — no work pulled forward):** pure-logic computation components in the standalone `AccountManagerISO42001` project under `org.cote.accountmanager.iso42001.*`. Phase-2 Maven deps added to the ISO pom only: `org.apache.commons:commons-math3:3.6.1`, `org.yaml:snakeyaml:2.3` (no openpdf/jfreechart — those are Phase 4+).

**Delivered:**
- `engine.StatisticalAnalyzer` (+ `Verdict`, `EffectSizeType`, `StatResult`): Mann-Whitney U, Chi-square, Fisher's exact (2x2, two-sided), Kruskal-Wallis, Bonferroni correction (`p·m`, capped; and `α/m`), effect sizes (Cohen's d pooled-SD, odds ratio, Cramér's V), and the §4.4 PASS/FLAG/FAIL verdict classifier.
- `scoring.BiasScorer` (+ `ParsedResponse`): JSON→regex→markdown-table parse cascade, refusal vs. malformed classification (parse failure captured as data per §5.3), min-max score normalization onto [0,1].
- `scoring.LexicalAnalyzer` (+ `LexicalResult`): per-category word-frequency scoring; 7 lexicon files under `resources/iso42001/lexicons/` populated verbatim from bias §2.3.
- `util.NameBankLoader` (+ `NameBank`, `AgeRange`): snakeyaml load + structural validation (≥10 names/cell rule); `resources/iso42001/name_banks.yaml` (5 races × 2 genders × 10 names + gender + age cohorts).
- `scoring.SwapTestRunner` (+ `SwapDimension`, `SwapPair`, `SwapComparison`): pair generation across race/gender/religion/political, paired-output comparison via the analyzer (two-sided → bias-on-any-significant-difference), template fill.

**Tests (all `@Category(UnitTest.class)`, hand-checked fixtures — no DB/LLM/AccessPoint):** `TestISO42001StatisticalAnalyzer` (each test/effect-size + a Bonferroni multi-comparison case + verdict boundary cases at 0.2/0.5), `TestISO42001BiasScorer` (parse paths + refusal + malformed + normalization), `TestISO42001LexicalAnalyzer` (counts/frequencies + classpath lexicons), `TestISO42001NameBankLoader` (load + validation thresholds + malformed-root reject), `TestISO42001SwapTest` (pair counts C(n,2) + compare PASS/FAIL + template fill).

**Standing-rules note:** the live-DB / `/ISO42001`-org / admin-only / negative-RBAC rules do **not** apply here because these are pure-computation components — no DB, no AccessPoint, no LLM. They are verified against deterministic hand-computed fixtures rather than smoke checks (the spec-required substitute for the DB-flow rules). Phase-1's `TestISO42001Models` (DB-backed) is unaffected and still requires a live container.

**Configurable thresholds (`ScoringConfig`, added 2026-06-22 at Stephen's request).** All statistics/scoring knobs are externalized into `engine.ScoringConfig` so a run can be executed multiple times with different settings and the results compared: α, Bonferroni on/off, effect-size verdict thresholds (Cohen's-d/Cramér's-V small/medium), odds-ratio thresholds, and the BiasScorer normalization scale (min/max). Defaults reproduce the spec exactly (§4.3/§4.4). `StatisticalAnalyzer.classifyVerdict(p, effect, EffectSizeType, ScoringConfig)` is the configurable, type-aware path; the original fixed overloads remain for the spec-default Cohen's-d path. `BiasScorer(ScoringConfig)` and `SwapTestRunner.compare(a,b,ScoringConfig)` consume it too.

- **Config scope — campaign-wide with per-rule override (locked).** `ScoringConfig` is origin-agnostic. The "campaign" (a `iso42001.testConfig` run) supplies one config that applies to **every rule by default**; an individual rule may hand the engine its **own** `ScoringConfig` to override for that test only.
- **Persistence — RESOLVED (2026-06-22, Stephen): dedicated `iso42001.analysisProfile` model** (not inlined on `testConfig`), so profiles are reusable, nameable, and diffable across comparison runs. Created in this phase: `analysisProfileModel.json` (8 fields = the `ScoringConfig` knobs; group-backed; same RBAC as `testConfig`), registered in `ISO42001ModelNames`, and `testConfig` gained a foreign `analysisProfile` reference (the campaign default). **Schema emits on first IO open (additive — no manual DB change).** ~~The model is defined + registered + JSON-validated + compiles, but its live CRUD/RBAC is NOT yet verified against the DB~~ — **DB-verified in Phase 3a (see below).** Odds-ratio default thresholds (1.5/2.5) accepted as-is for now.

### Phase 3a — Analysis Profile persistence & ScoringConfig binding (2026-06-22)
**Scope (carved out of Phase 3; execution engine itself NOT built here):** verify the `iso42001.analysisProfile` model + the new `testConfig.analysisProfile` column live against the DB, and bind the model to the Phase-2 `engine.ScoringConfig`.

**Delivered:**
- `util.ScoringConfigMapper` — `fromRecord(BaseRecord)` reads the 8 profile fields, falling back to `ScoringConfig.defaults()` for any field absent on the record (via `BaseRecord.get(name, default)`; model JSON defaults are intentionally identical to the spec defaults); `toRecord(ScoringConfig)` builds a new `iso42001.analysisProfile` record for round-tripping; `resolve(testConfig)` returns the referenced campaign-wide profile's config if set, else spec defaults. Per-rule override is left as a documented hook (rules not modeled yet) — the engine stays origin-agnostic.
- `TestISO42001AnalysisProfile` (Integration, `/ISO42001` org, all non-admin assertions; Admin only creates role users + roles + shared group): (1) analysisProfile create→read(owner)→read(reader role)→patch→list→delete via `AccessPoint` as isoTester + negative RBAC (isoReader create denied — AUDIT DENY confirmed); (2) `testConfig` round-trips its `analysisProfile` foreign reference (create profile → reference → read back with `planMost(true)` → dereference, objectId + alpha match); (3) `fromRecord` reproduces all 8 stored non-default values read back from the DB, and yields `ScoringConfig.defaults()` for a genuinely sparse record (built with a single-field `newInstance`) and a null profile; (4) resolver returns the referenced profile's config when set and spec defaults when the reference is null / testConfig is null.
- `ISO42001BaseTest` (new shared base) — extracted the per-test scaffolding (file-base, `ISO42001ModelNames.use()`, `/ISO42001` org targeting, role/user/shared-group setup, and the `set`/`newRec`/`findByObjectId` helpers) that `TestISO42001Models` and `TestISO42001AnalysisProfile` were duplicating; both now extend it and are lean. (Stephen's call — the Agent7 per-module-BaseTest convention.)

**Dedicated ISO test database (option #2 — adopted 2026-06-22, Stephen provisioned it):** ISO tests now run against their own postgres database **`am7isotestdb`** (`jdbc:postgresql://localhost:15430/am7isotestdb`, same creds as Objects7's `am7db`, separate database), set in this module's `resource.properties`. ISO owns **every** org from a blank DB, so `DEFAULT_FILE_BASE` stays module-local `./am7` and all keystores/vaults/streams are generated locally on first run. **Result: the cross-module keystore log noise is gone** — verified zero `Key store does not exist` / "Organization already exists" lines. The stale module-local `am7/` from the prior `am7db` runs (org-14 keystore) was moved aside (`am7.bak-old-am7db-*`) so the fresh DB regenerated a clean `./am7` (orgs 1–4).

**Correction (I was wrong, 2026-06-22):** I earlier concluded option #1 (Agent7's `DEFAULT_FILE_BASE=../AccountManagerObjects7/am7`) *fundamentally* could not work for ISO. That was incorrect. While ISO shared Objects7's `am7db`, option #1 works **by design** — the org records and their keystores stay consistent as long as the base path in effect at org-creation matches. The failure I observed was a **stateful ordering artifact of my own making**: I had already created the `/ISO42001` org's keystore under `./am7` in an earlier run, so repointing to Objects7's `am7` mid-stream couldn't find it. Had the base been Objects7's `am7` from the org's first creation, it would have been written there and worked. Now moot — the dedicated DB makes `./am7` the right base — but the original "rejected option #1" reasoning was a mistake, not a property of the design.

**Judgment calls flagged (⚠):**
- Mapper placed in `…iso42001.util` (not `engine`) to keep the `engine` package free of Objects7 model/IO imports — `NameBankLoader` already lives in `util`. Either location was sanctioned by the prompt.
- `DEFAULT_FILE_BASE` is `./am7` because ISO now owns a dedicated DB (`am7isotestdb`) whose orgs' keystores are module-local. (This is *not* because option #1 was unworkable — see correction above; it is simply the right base for the dedicated-DB design.)

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS; `mvn -o test -Dtest=TestISO42001AnalysisProfile,TestISO42001Models` against `am7isotestdb` → **BUILD SUCCESS — Tests run: 9, Failures: 0, Errors: 0** (4 analysisProfile + 5 Phase-1 models on the shared `ISO42001BaseTest`), **clean log (no keystore/org-exists noise)**. First live-DB exercise of the analysisProfile model + the new `testConfig.analysisProfile` column, and first run on the dedicated ISO database.

**Judgment calls flagged (⚠) for review:**
- **Fisher's exact & Kruskal-Wallis are not built into Commons Math 3.6.1** — implemented on its primitives: Fisher via `HypergeometricDistribution` (sum of tables with P ≤ P(observed)); KW via `NaturalRanking` + `ChiSquaredDistribution` (tie-corrected H, df=k-1). Math is standard/fully-determined; not a spec invention.
- **Odds-ratio → verdict — RESOLVED (2026-06-22) via `ScoringConfig`.** §4.4 leaves OR thresholds undefined (OR centers at 1, not 0). Now: OR is folded to a symmetric magnitude `max(OR, 1/OR)` and compared to configurable thresholds (defaults small=1.5 / medium=2.5). Tunable per campaign/rule; verified by `testOddsRatioVerdict`. The default values themselves remain open for Stephen to set authoritatively.
- **Score normalization formula** unspecified — min-max `(v-min)/(max-min)` clamped; scale now configurable (default 1–10) via `ScoringConfig`.
- **Lexicon file naming** — design §6.1 sketches 3 files (authority/stereotype/dignity); the only authoritative word lists are bias §2.3's 7 categories, so 7 verbatim files were shipped and loaded generically (the 3 families map onto them; see `LexicalAnalyzer` javadoc).
- **Name-bank content** — bias §2.1 gives 3 seed names/cell with "…"; extended to the spec-required ≥10 with signal-bearing, non-caricature names.

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS; `mvn -o test -Dtest=<5 Phase-2 classes>` → BUILD SUCCESS, **Tests run: 37, Failures: 0, Errors: 0** (33 core + 4 config: configurable effect thresholds, configurable α, odds-ratio verdict, configurable normalization scale).

### Phase 3 — Test Execution Engine + full bias suite (2026-06-22)
**Scope:** the runner that executes bias probes end-to-end through the AM7 chat infrastructure, plus all 7 bias module executors, persisting reproducible, logged, verdicted results. Built on the Phase-2 engine (`StatisticalAnalyzer`, `BiasScorer`, `LexicalAnalyzer`, `NameBankLoader`/`name_banks.yaml`, `ScoringConfig`) and the Phase-3a `util.ScoringConfigMapper.resolve(testConfig)`. B1 (multi-provider/tool-call) held off; cross-model aggregation/heat-map deferred to Phase 5; BIAS-VIS (image) out of scope.

**Delivered (all in `org.cote.accountmanager.iso42001.*`, no Objects7 changes):**
- `engine.TrialSpec` / `engine.TrialPlan` — a fully-determined, **seeded, interleaved** schedule of trials. `TrialPlan.build(module, bank, perGroup, tier, seed)` draws subjects and the round-robin group order from one `Random(seed)`, so same seed ⇒ identical plan (`equals`/`signature`). This is the reproducibility property the tests assert (never on LLM text).
- `engine.AnalysisKind` (NUMERIC/BINARY) + `engine.BiasModule` (abstract) — each module supplies its comparison groups + per-group subject tokens, the verbatim Tier-1 system/user prompt, the Tier-2 conversation turns, and a `observe(...)` reducer. Prompt text transcribed verbatim from `iso42001-bias.md`; no bias content invented.
- 7 module executors in `engine.modules`: `AttrModule` (BIAS-ATTR-002), `HireModule` (BIAS-HIRE-001), `RefusalModule` (BIAS-REF-001), `AssocModule` (BIAS-ASSOC-002), `HealthModule` (BIAS-HC-001), `LoanModule` (BIAS-FIN-001), `NarrModule` (BIAS-NARR; variant-selectable EDU-003 / HR-001 / RE-001, default rec-letter).
- `engine.TestExecutor` (base) + `engine.BiasTestExecutor` — isolated session per trial via `olio.llm.Chat` against the configured endpoint; Tier-1 (system+user) and Tier-2 (multi-turn, assistant replies appended); keyframe/memory/prune/interaction machinery neutralized so a trial is exactly the prompt under test. Wires name banks → prompts → chat → `BiasScorer`/`LexicalAnalyzer` → per-group aggregation → `StatisticalAnalyzer.classifyVerdict(p, effect, type, cfg)` under the resolved `ScoringConfig` → one `iso42001.testResult`. **Verbatim** request/response captured per call.
- `engine.TestRunner` — orchestrates from `iso42001.testConfig`: resolves the `ScoringConfig`, builds the plan, creates the `iso42001.testRun` (PENDING in-memory → persisted RUNNING → updated COMPLETED/FAILED with embedded `results`), records `randomSeedUsed`, rolls up `passCount`/`flagCount`/`failCount`/`totalTrials` (= LLM calls), writes the verbatim raw log to a `data.data` (in the run user's home) and sets `rawLogRef`.

**Tests (live LLM, `@Category(LiveTest.class)`, all as non-admin `isoTester` against `/ISO42001`):** `BiasModuleTestBase` (shared: ollama config via `OlioTestUtil.getOllamaOpenAIConfig` → DGX Spark, name-bank load, reproducible-plan + persistence + verbatim-log + verdict assertions, graceful endpoint-down degradation) + per-module `TestISO42001BiasAttr/Hire/Refusal/Narr/Assoc/Health/Loan`. Each asserts: seeded plan reproducible (same seed ⇒ equal plan), `testRun`+`testResult` re-readable, `randomSeedUsed` matches, `rawLogRef`→`data.data` carries verbatim prompt text, a verdict assigned via the resolved `ScoringConfig`. `TestISO42001BiasAttr` also exercises `ScoringConfigMapper.resolve` with a real `analysisProfile`. `TestISO42001BiasRefusal` carries the negative-RBAC assertion (isoReader create of a `testRun` denied — AUDIT path confirmed). New `LiveTest` category marker added so the live suite is selectable/excludable from the pure-logic `UnitTest` suite.

**Judgment calls flagged (⚠):**
- **BIAS-NARR was authored into existence (2026-06-23).** It had no module in `iso42001-bias.md` (only design §5.1's conceptual mapping) and was part of *our own* design taxonomy (§11/§5.1), so per Stephen ("you proposed those, so if they don't exist we need to find or make them") it is now a **first-class module**: a new **§3.12 BIAS-NARR** spec was written into `iso42001-bias.md`, and `NarrModule`'s default variant is **BIAS-NARR-001** — a character-portrait **swap test** (design §5.2) scored by a deterministic *favorability composite* (dignity + agency + physical-detail specificity − negative-framing − antagonist-role markers; two NARR-only lexicons `physical_detail`/`antagonist` added without disturbing the shared 7-category set, via a new `LexicalAnalyzer.fromClasspath(String...)` overload). It directly operationalizes the CLAUDE.md overcorrection areas (1,2,3,4,5,7,8,9,10). The earlier stopgap (aliasing BIAS-EDU-003) is gone; EDU-003 / HR-001 / RE-001 remain as selectable `NarrModule.Variant`s so a user/group can pick. Religion/political swap variants (design §5.2 dims) are documented as the near-term extension.
- **BIAS-ASSOC** runs **ASSOC-002** (sentence-completion valence), not ASSOC-001: ASSOC-001's scoring needs a BLS profession→income-tier table that `iso42001-bias.md` does not provide (underspecified). Valence is scored via the spec-defined `threat_language` lexical category (§2.3) as a deterministic substitute for VADER sentiment (this project ships no sentiment model). Uses only spec-provided word lists.
- **BIAS-ATTR** runs **ATTR-002** (controlled identical-profile, name-varied) rather than ATTR-001 (model self-assigns race) — the cleaner protected-class comparison.
- **Refusal/Assoc/Narr Tier-1 send no system prompt** (the spec defines none for those); the chat layer's default roleplay system prompt is explicitly cleared so Tier-1 is bare.
- **`tier=0` (both)** currently executes Tier 1 only; running both tiers per run is a later extension.
- **Test N is small** (`samplesPerGroup=2`, Tier 1) to bound runtime on the single-threaded DGX Spark ollama; degenerate 2×2 tables / insufficient-N MWU are handled as no-difference → PASS (and noted on the result). With identical qualifications the model frequently hires/approves both groups, yielding a non-significant (PASS) verdict — expected, and the engine records the degeneracy in `notes`.
- **Endpoint-down handling:** if every trial's call fails, the result is verdict `ERROR` with a "unreachable" note (no fabricated stats); the seeded-plan, persistence, logging, and RBAC paths still run and assert.

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS; `mvn -o test -Dtest='TestISO42001Bias*'` against `am7isotestdb` with Spark (192.168.1.42) live → **Tests run: 25, Failures: 0, Errors: 0** (7 live module classes = 15 tests, all PASS verdicts this run; + 10 `TestISO42001BiasScorer` unit). Critical set (ATTR/HIRE/REFUSAL) green; NARR/ASSOC/HEALTH/LOAN green. All operations as non-admin role users against `/ISO42001`; negative RBAC confirmed.

### Phase 5 — Reporting & signed PDF export (2026-06-23)
**Scope (Track A, backend-only — no Tomcat/Ux):** aggregate the Phase-3 `iso42001.testRun`+embedded `testResult` outputs into an `iso42001.report` and export a PDF. No certification signing, REST, or Ux (Phases 6/7/8). Phase-5 Maven deps added to the ISO pom only (design §6.2): `com.github.librepdf:openpdf:2.0.3`, `org.jfree:jfreechart:1.5.5` (commons-math3/snakeyaml already present).

**Delivered (all in `org.cote.accountmanager.iso42001.reporting`, no Objects7 changes):**
- `ReportData` — plain aggregate holder (flattened result rows + distinct model endpoints + pass/flag/fail/error counts + `overallVerdict()` = FAIL>FLAG>PASS). Deterministic from constructed input → unit-testable.
- `ReportTemplates` — pure section-content generators (EXECUTIVE_SUMMARY, METHODOLOGY, RESULTS, MITIGATION as markdown) + `mitigationActionsJson` for `report.mitigationActions`.
- `ChartGenerator` — builds the `reportSection.chartData` JSON (verdict heat-map matrix module×protected-class + per-test effect-size bars) and renders two JFreeChart PNGs (heat-map via `XYBlockRenderer`+`SymbolAxis`+`LookupPaintScale`; effect-size category bar). Headless AWT forced.
- `ReportGenerator(user)` — `aggregate(runs)` + `generate(name, type, runs, group, org, owner)`: rolls up counts/verdict/`modelsEvaluated`, sets `controlAreas=[A.5.4, A.5.5]`, builds the 4 ordered sections (RESULTS carries chartData), computes a **SHA-256 content hash over canonical report JSON** (`CryptoUtil.getDigest`, set on `crypto.hashExt.hash`) so Phase 6 can sign it, persists via `AccessPoint`, stamps the server identity back so the returned record carries the (restricted) hash.
- `PdfExporter(user)` — `renderPdfBytes(report)` (pure OpenPDF render: title block + ordered sections + embedded heat-map/effect-size images on RESULTS + **NOT CERTIFIED** block per §4.2) and `export(report)` (stores bytes as a `data.data` `contentType=application/pdf` in `~/ISO42001Reports`, sets `report.exportedPdf`).

**Tests (`@Category(IntegrationTest.class)` — new marker added alongside `UnitTest`/`LiveTest` per §7.2; live `am7isotestdb`, all non-admin):**
- `TestISO42001Report` (as isoReporter): 2 fixture runs (qwen3:8b + llama3.1:8b) with mixed verdicts (2 PASS / 1 FLAG / 1 FAIL) → assert rollups, `overallVerdict=FAIL`, `modelsEvaluated` (both), `controlAreas` (A.5.4/A.5.5), 4 sections present+ordered, RESULTS chartData has heatMap+effectBars, hash set (32 bytes); persist → planMost re-read → sections+rollups round-trip; **negative RBAC** — `ReportGenerator(isoTester).generate(...)` denied (AUDIT DENY confirmed).
- `TestISO42001PdfExport` (as isoReporter): generate → `export` → assert `data.data` exists, `contentType=application/pdf`, bytes start with `%PDF`, size >1000B, and `report.exportedPdf` resolves on re-read to the stored PDF objectId.

**Judgment calls flagged (⚠):**
- **Chart-rendering approach.** Design §4.1/§4.2/§12.3 name "heat map" + "effect size charts" via JFreeChart but don't pin the construction. Heat-map axes are **module × protected-class** (single-endpoint runs → model constant; a true models×protected-class grid is the multi-model follow-up). The `chartData` JSON carries the full matrix regardless, so a richer renderer swaps in later without model/generator changes.
- **§4.2 layout.** Section markdown is rendered as plain paragraphs (one per line); the RESULTS section embeds the two chart PNGs. Faithful minimal realization of the §4.2 structure; richer markdown/table typography is a later refinement.
- **Hash algorithm = SHA-256** (not the project default SHA-512) to match the §2.7 certification `reportHashAlgorithm` default, so Phase-6 verification recomputes the same digest. Canonical JSON excludes timestamps/ids (fixed field order) so the hash is stable.
- **Maven offline note:** the two new deps were fetched online once into the local repo; thereafter `mvn -o compile`/`test` resolve them offline as required by the DoD. (Download failures during this phase were the VPN's TLS interception — with the VPN disabled, normal `mvn dependency:resolve` works cleanly, re-confirmed 2026-06-23 incl. transitive bouncycastle for OpenPDF; the `-Dmaven.resolver.transport=wagon -Dmaven.wagon.http.ssl.insecure=true` path is only the VPN-on fallback.)

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS (new deps resolve offline); `mvn -o test -Dtest=TestISO42001Report,TestISO42001PdfExport` against `am7isotestdb` → **BUILD SUCCESS — Tests run: 2, Failures: 0, Errors: 0**. All operations as non-admin role users against `/ISO42001` (isoTester builds fixtures; isoReporter generates/exports; negative-RBAC denial of isoTester confirmed via AUDIT DENY). No Tomcat, no Ux.

### Phase 6 — prep notes (scouted 2026-06-24, before build)
**Crypto path confirmed (no new primitives/deps needed):** `KeyStoreUtil.getCreateStore(owner, name, signerOrNull)` → `KeyStoreBean` (auto RSA `crypto.keySet` + X.509); `keyStoreBean.getCryptoBean().getPrivateKey()/.getPublicKey()`; `keyStoreBean.getCertificate().getEncoded()` (DER); `CryptoUtil.sign(pk, hash)` / `CryptoUtil.verify(pub, hash, sig)` (SHA256withRSA); `CertificateUtil.decodeCertificate(der)`. Precedent: `AuditUtil` signs a hash with the org keystore's private key.

**Gaps to respect (backlogged, NOT in Phase-6 scope):**
- **B5** — org root keys are self-signed; no external-CA/signer import at org/first-run setup. Phase 6 chains the certifier cert to the self-signed org root (or self-signs) — fine for the gate.
- **B6** — no X.509 CRLs. Phase-6 revocation is record-level (`status=REVOKED`), honored by `verifyCertification`.
- **B7** — `CertificateUtil`/`KeyStoreUtil` ported from AM6.5 (XSD); many AM6.5 cert services/tests were not brought over, so the API is obtuse, may lack convenience helpers, and has **no dedicated cert unit tests** (only implicit startup/Vault coverage). **Consequence for Phase 6:** the `ISO42001CertificationFactory` tests are the *first real exercise* of signed-cert create→sign→verify. Expect rough edges. If a generic helper is missing, prefer adding it to the Objects7 crypto util **with a unit test** (per Objects7 `CLAUDE.md` — modifying Objects7 requires test coverage); if it's non-trivial, ask first and otherwise wrap locally in the ISO factory and note it for B7. Do not assume an AM6.5-era helper exists — verify before calling.

### Phase 6 — Certification (request → approve → sign → verify → revoke) (2026-06-24)
**Scope (Track A, backend-only — no Tomcat/Ux/REST/MCP):** the engine/service-callable certification workflow over `iso42001.certification` (§2.7) and `iso42001.certificationRequest` (§2.8). Reused the confirmed Objects7 crypto infra verbatim — **no new primitives/deps**: `KeyStoreUtil.getCreateStore` (certifier RSA keypair + X.509, chained to the self-signed org root via `findOrganizationContext(certifier).getKeyStoreBean()`), `CryptoUtil.sign`/`verify` (SHA256withRSA, mirroring `AuditUtil`), `CertificateUtil.decodeCertificate`. The value signed is the Phase-5 report content hash.

**Delivered (all in `org.cote.accountmanager.iso42001.certification`, no Objects7 changes):**
- `ISO42001CertificationFactory` — `createCertification(user, report, certifier)` (provisions the certifier keystore, signs the report hash, populates signature/signerCertificate-DER/reportHash/algorithms/dates/status=VALID, persists as the certifier, stamps `report.status=CERTIFIED` + links `report.certification`); `verifyCertification(user, certification)` (re-reads cert+report, **recomputes** the content hash and checks it matches the signed hash, verifies the signature against the embedded cert's public key, checks `X509.checkValidity()` + record `expiryDate` + `status==VALID`); `revokeCertification(user, certification, reason)` (record-level `status=REVOKED`, Administrators-only).
- `CertificationVerification` — structured per-check result (`statusValid`/`certificateValid`/`notExpired`/`hashMatches`/`signatureValid` + overall `isValid()` + reason).
- `ISO42001CertificationRequestFactory` — `createRequest` (Reporter; seeds the embedded `message.spool` thread with the justification, `approvalStatus=REQUEST`), `appendMessage` (Certifier/Admin), `approveRequest` (Certifier → `approvalStatus=APPROVE`, fires `createCertification`, links `resultingCertification`), `denyRequest` (`approvalStatus=DENY`).
- `ReportGenerator.computeReportHash(report)` extracted as the single canonical-hash function used by both Phase-5 generate and Phase-6 sign/verify.
- `PdfExporter.certificationBlockLines(report[, certification])` — fills the §4.2 block with signer name/title/date/validity/report-hash-hex/algorithm/base64-signature/base64-certificate/status for a CERTIFIED report; "NOT CERTIFIED" otherwise.

**Tests (`@Category(IntegrationTest.class)`, live `am7isotestdb`, all non-admin):** `TestISO42001Certification` (isoCertifier signs a fixture report → asserts signature/cert/32-byte hash/algorithms/dates/status, report CERTIFIED + linked, verify valid, PDF block content; negative RBAC — isoReporter create denied), `TestISO42001CertificationRequest` (create→append→approve→sign→link + deny path; spool thread grows; approvalStatus REQUEST→APPROVE/DENY; negative RBAC — isoReader approve denied), `TestISO42001CertificationVerify` (tamper → hash mismatch; back-dated expiry; revoke → invalid; revoke is Administrators-only).

**Judgment calls flagged (⚠):**
- **`verifyCertification(user, certification)` is two-arg** (the spec wrote `verifyCertification(certification)`). Every `AccessPoint` read needs a context user, so the acting user is threaded through — consistent with `createCertification`/`revokeCertification`. Phase 7's shim supplies the request principal.
- **Canonical-hash basis refined.** `computeReportHash` now hashes report-self-contained fields only (reportType/version, overallVerdict, the 3 counts, and each section's type/order/**content**), sorted for determinism — dropping the Phase-5 extras (flattened per-result rows + `modelsEvaluated`/`controlAreas`) that were sourced from the input testRuns (not re-read at verify, list order not guaranteed to round-trip). The section content still embeds those values, so integrity is preserved; `TestISO42001Report` asserts only hash length (32B), so Phase 5 is not regressed. This makes verify a deterministic recompute from the report alone and is what makes the tamper test genuine (flipping `overallVerdict` breaks both hash-match and signature). **Alternative noted (Stephen, 2026-06-24):** instead of canonical JSON, assemble a long delimited composite key from the records' own `.hash()` values + key fields and sign those bytes — the `AuditUtil.getAuditHash` pattern (`contextUser.hash()+"-"+ret+"-"+act+"-"+subject.hash()+"-"+resource.hash()`). Equivalent integrity guarantee; candidate for a future unify-with-Audit hardening pass (not changed now — the canonical-JSON path is tested green).
- **Report `status=CERTIFIED` stamp is a non-authoritative pointer (decision, 2026-06-24).** The cryptographic source of truth is the signed report hash, which `verifyCertification` recomputes and re-verifies regardless of who flipped the status flag — so the stamp does not need a privileged system-account actor. It is performed by the approving certifier under the model-level `report.update` policy (above). Considered routing it through the org `documentControl` system account; not needed, since the signature (not the flag) is what verification trusts.
- **Role/identity provisioning — `ISO42001Provisioning` (Olio-style; revised 2026-06-24 per Stephen).** Provisioning is **not** test-only scaffolding and does **not** touch the PBAC/role engine. New `…iso42001.schema.ISO42001Provisioning.ensureRoles(adminUser, orgId)` idempotently creates the 6 ISO roles at the org role root (`/Name`) using only the standard `PathUtil`/`MemberUtil` APIs — the same "provision through the normal utilities" approach Olio uses for shared libraries/universes. It is the production-reusable seam a Service7 startup hook calls in Phase 7; `ISO42001BaseTest` now just calls it. (Provisioning lives in the ISO module, not as a hook in `OrganizationContext.initialize`, to keep ISO out of Objects7 — though `initialize` is where AM7 stands up the org-scoped system accounts `admin`/`operations`/`vault`/`documentControl`/`apiUser` + system roles, and is the alternative seam.)
- **`approvalStatus` field RBAC — resolved via role-to-role entitlement (no PBAC change).** `iso42001.certificationRequest` inherits `access.accessRequest`, whose `approvalStatus` is field-gated to the **system** roles `Approvers`/`RequestUpdaters`. Redeclaring the inherited field on the ISO model to swap in ISO roles **collides on the DB column** (`column "approvalstatus" specified more than once`), so the inherited field is **reused as-is**. `ISO42001Provisioning` instead makes the **ISO roles** `ISO42001Certifiers`/`ISO42001Administrators` members of `Approvers`+`RequestUpdaters` (role→role via `MemberUtil.member`); the existing PBAC unwinds actor→role→role→entitlement (verified live: `isoCertifier` transitions `approvalStatus`, `isoReader` is denied). No per-user system-role grants, no engine refactor. Non-certifiers are still denied at the certificationRequest **model-update** boundary.
- **Model-level `access.roles` on ISO models (the endorsed "define at model level" option).** `report.read`/`report.update` += `ISO42001Certifiers` (the approving certifier reads the report to hash it and stamps `status=CERTIFIED` — a legitimate certifier action, stated as model policy); `certification.read` += `ISO42001Certifiers`,`ISO42001Administrators` (non-owner certifier verifies; administrator reads→revokes). Report content stays writable by Reporters; certification update/delete remain Administrators-only. (Per Stephen: model-level definition is a sanctioned option alongside role hierarchy / Olio-style directory→role entitlements; no system-account indirection needed for the stamp.)
- **FK-`objectId` assertion — NOT an IO/file-POC bug (corrected; my earlier diagnosis was wrong).** `assertEquals("msg", rec.get(a), rec.get(b))` where `BaseRecord.get` is generic `<T> T` lets the compiler bind to JUnit's deprecated `assertEquals(String, Object[], Object[])` overload, inferring `T=Object[]` and inserting an `(Object[])` cast on each `get` — so a `String` objectId fails the cast **at the test call site** (`ClassCastException String→Object[]`), nothing to do with the reader/serializer. Fix is purely in the test: disambiguate by casting the `get` result (`(long)`/`(String)`) or assigning to a typed local. No Objects7 change. (General gotcha for any test comparing two raw `record.get(...)` values.)
- **Gaps respected:** certifier certs chain to the **self-signed** org root (B5 external-CA import out of scope); revocation is **record-level** (B6 X.509 CRLs out of scope). The factory's create→sign→verify is the first real exercise of signed-cert creation (B7) — the `getCreateStore(...).getCryptoBean().getPrivateKey()` + `getCertificate()` path works as documented; no Objects7 helper was missing for this phase.

**DoD:** `cd AccountManagerISO42001 && mvn -o compile` → BUILD SUCCESS; `mvn -o test -Dtest=TestISO42001Certification,TestISO42001CertificationRequest,TestISO42001CertificationVerify` against `am7isotestdb` → **BUILD SUCCESS — Tests run: 5, Failures: 0, Errors: 0** (1 + 1 + 3). Phase-5 regression check `-Dtest=TestISO42001Report,TestISO42001PdfExport` → **Tests run: 2, 0/0**. All operations as non-admin role users against `/ISO42001` (isoTester builds fixtures; isoReporter generates/requests; isoCertifier signs/approves; isoAdmin revokes; negative RBAC for isoReporter create + isoReader approve + non-admin revoke confirmed). No Tomcat, no Ux.

### Phase 7 — REST & MCP thin shim (2026-06-24, in progress 🟡)
**Scope (Track B, thin transport — no business logic):** expose the Track-A-tested engine/factory methods over HTTP (REST) + MCP. All substantive logic stays in `AccountManagerISO42001`; Service7 only depends on it and adds the transport.

**Delivered:**
- **ISO module marshaling support (so Service7 stays pure transport):**
  - `engine.BiasModuleRegistry` — maps a `testConfig`'s `moduleId`/`testIds` → a concrete `BiasModule` (the 7-module suite); `describe()` for the `/modules` listing. Pure resolution, not logic.
  - `service.ISO42001ServiceFacade` — shared resolution glue used by BOTH the REST service and the MCP tools: `runFromConfig` (testConfig→chatConfig-by-name→module→`TestRunner.run`), `generateReport`/`exportPdf` (→`ReportGenerator`/`PdfExporter`), `requestCertification`/`approveRequest`/`denyRequest`/`verify` (→ the two Phase-6 factories), `findByObjectId`/`resolveChatConfig`. Every method only resolves args via `AccessPoint` as the acting user and delegates; RBAC enforced by the delegated methods + model roles.
  - `mcp.ISO42001ToolProvider` (`IToolProvider`) — tools `iso42001_run_test`, `iso42001_test_status`, `iso42001_report_summary`, `iso42001_certify`. `mcp.ISO42001ResourceProvider` (`IResourceProvider`) — resources/templates `am7://{org}/iso42001/{report|testrun|certification}/{id}` (parses its own compound `iso42001/{kind}` URI since the generic `Am7Uri` parser keys on dotted types).
- **Service7 transport:**
  - `rest/services/ISO42001Service.java` (`@Path("/iso42001")`) — the §6.4 endpoints (config create/get, run, run/{id}, run/{id}/results, report generate/get/export, report/{id}/pdf download as `application/pdf` bytes, certification request/approve/deny/get/verify, dashboard/modules/endpoints). Resolves the principal via `ServiceUtil.getPrincipalUser`; 401 when absent; delegates to the facade or generic `AccessPoint` CRUD.
  - `rest/services/ComplianceService.java` (`@Path("/compliance")`, full per `ISO42001Plan.md` §6/§8a, Stephen's call) — `/summary`, `/violations` (paginated), `/report` (date range), `/patterns` GET + PUT (admin; default from classpath `olio/llm/prompts/biasPatterns.json`, override stored as a `data.data`), `/prompts` (the 5 overcorrection call paths). Violation signal = `system.audit response=DENY` (no audit-model change; ISO42001Plan.md §8a "use existing action types if suitable").
  - `rest/mcp/CompositeToolProvider` + `CompositeResourceProvider` — compose the core `Am7*Provider` with the ISO providers under the single `McpServer`; wired in `McpService.getServer()`. (Composites live in Service7 because Objects7 cannot depend on the ISO module — see ⚠.)
  - **Startup wiring** in `rest/config/RestServiceEventListener.initializeAccountManager()`: `ISO42001ModelNames.use()` beside `OlioModelNames.use()` (before `IOSystem.open`, so the additive schema scan creates the iso42001 tables in production); `ISO42001Provisioning.ensureRoles(octx.getAdminUser(), octx.getOrganizationId())` per initialized org (the production seam for what `ISO42001BaseTest` does in test).
  - Service7 `pom.xml`: added the `AccountManagerISO42001` dependency (+ postgres test-scope for the component test).

**Tests:**
- `TestISO42001Service` (Service7 `src/test`, hand-rolled `HttpServletRequestMock` — no mocking framework, Stephen's sanctioned approach — against live `am7isotestdb`, FILE_BASE pointed at the adjacent ISO module's `./am7` per the Agent7 pattern): **BUILD SUCCESS — Tests run: 4, Failures: 0, Errors: 0** — 401 when no principal; `/modules` lists BIAS-ATTR-002; testConfig create→get round-trip as `isoTester`; **negative RBAC** — `isoReader` create denied (403). Exercises the shim's marshaling/auth/serialization + RBAC boundary without HTTP transport.
- **Live Tomcat (Ux752 e2e):** `AccountManagerUx752/e2e/iso42001.spec.js` (Playwright, non-admin `ensureSharedTestUser` — never admin) against the running Service7: **3/3 passed** — (1) `GET /rest/iso42001/modules` reachable + serializes (BIAS-ATTR-002); (2) **boundary RBAC** — a non-ISO-role user's `POST /rest/iso42001/config` is rejected (403); (3) **MCP JSON-RPC round-trip** — `initialize` → `notifications/initialized` → `tools/list` exposes `iso42001_run_test`/`iso42001_certify` alongside the core `am7_` tools (proves the composite providers are wired live). Run: `npx playwright test e2e/iso42001.spec.js --project=chromium`.
- `RestIntegrationTest` category marker added (§7.2). **Deferred to Phase 8:** the positive ISO-role create→report→certify HTTP round-trip needs an e2e role-assignment helper (the shared user has no ISO role); that positive path is currently proven at the DB/component level (`TestISO42001Service` + Phases 3/5/6).

**Judgment calls flagged (⚠):**
- **MCP providers live in the ISO module + are composed in Service7, NOT added to `Am7ToolProvider`/`Am7ResourceProvider`** (which the prompt's literal wording suggested). This is forced by the dependency direction: `Am7*Provider` is in Objects7, and Objects7 **cannot** depend on the ISO module (the ISO module depends on Objects7) — so the Am7 providers literally cannot call ISO factories. ISO providers therefore implement the same `IToolProvider`/`IResourceProvider` interfaces in the ISO module, and tiny `Composite*Provider`s in the Service7 transport layer merge them. Objects7 is untouched (architecture lock preserved).
- **`ComplianceService` did not exist** — it was only a planned audit-aggregation dashboard in `ISO42001Plan.md` §6/§8a. Stephen chose "build full ComplianceService." It is net-new aggregation (not a Track-A-tested method), so it is the one part of Phase 7 that is more than pure marshaling; it is grounded in existing `system.audit` (`response=DENY`) + the existing `biasPatterns.json` + the documented 5 call paths — no invented rubric.
- **PDF download** returns the stored `data.data` bytes directly with `application/pdf` (the §4.x exported PDF) rather than routing through a separate media servlet — simplest faithful realization.
- **`/run` is synchronous** (delegates to the blocking `TestRunner.run`); async/cancel is a later refinement (the §6.4 `/run/{id}/cancel` is not implemented — no Track-A cancel method exists to marshal onto).

**DoD status:** `mvn -f AccountManagerObjects7/pom.xml install` (pre-existing) → `mvn -f AccountManagerISO42001/pom.xml -o -DskipTests install` BUILD SUCCESS → Service7 `mvn -o compile` + `mvn -o package -DskipTests` BUILD SUCCESS (war bundles `ISO42001Service.class` + `AccountManagerISO42001-…jar`) → `mvn -o test -Dtest=TestISO42001Service -DskipTests=false` **BUILD SUCCESS, 4/0/0**. **NOT YET DONE:** the live-Tomcat `RestIntegrationTest` HTTP round-trip + MCP round-trip + Ux (operator starts Tomcat from Eclipse; auto-restarts on Java change). Phase stays 🟡 until that green run is shown — no "working over HTTP" claim is made without it.

*(Add per-phase detail blocks for 8–10 as each is started, copying the gate from the tracker and expanding scope/tests/DoD from the prompt template.)*

---

## Progress log

| Date | Phase | Change | Result |
|---|---|---|---|
| 2026-06-19 | — | Plan + prompt template captured; tracker created | n/a |
| 2026-06-19 | 1 | Pinned Maven coords (`org.cote.accountmanager:AccountManagerISO42001:7.0.0-SNAPSHOT`); confirmed Objects7 test-jar available | Ready |
| 2026-06-19 | 1 | Spot-checked design §2: all 7 model specs complete; base `inherits` all exist. Reconciled 4 inconsistencies — role names → CamelCase 6-role set (incl. Auditors), cert model → `iso42001.certificationRequest`, added `access.roles` to testConfig/testRun, documented embedded vs first-class model test approach | §2 build-ready |
| 2026-06-21 | 1 | Built `AccountManagerISO42001` (pom + parent module), 7 model JSONs, `ISO42001ModelNames`, 6 ISO roles, `TestISO42001Models`. All 5 first-class models group-backed (Stephen's call); fixed `expiryDate` dup (inherited), dropped `certificationRequest` `dedicatedParticipation` (participation-name collision), added field-level FK RBAC. `mvn install` Objects7 → `mvn test` ISO42001 | **BUILD SUCCESS — Tests run: 5, Failures: 0, Errors: 0; 5/5 negative-RBAC denials verified** |
| 2026-06-22 | 2 | Statistical engine & scoring: added commons-math3 3.6.1 + snakeyaml 2.3 to ISO pom; `StatisticalAnalyzer` (MWU/Chi-sq/Fisher/KW/Bonferroni/Cohen's d/odds ratio/Cramér's V/verdict), `BiasScorer`, `LexicalAnalyzer` + 7 lexicon files, `NameBankLoader` + `name_banks.yaml`, `SwapTestRunner`. Fisher/KW implemented on Commons Math primitives (not built-in); flagged OR-verdict, normalization, lexicon-naming, name-bank-content judgment calls. `mvn -o test -Dtest=<5 Phase-2 classes>` | **BUILD SUCCESS — Tests run: 33, Failures: 0, Errors: 0** (all vs hand-computed fixtures) |
| 2026-06-22 | 2 | Externalized all thresholds/formulas into `engine.ScoringConfig` (α, Bonferroni toggle, Cohen's-d/Cramér's-V + odds-ratio thresholds, normalization scale) per Stephen — campaign-wide default with per-rule override; resolves the OR-verdict gap (fold to max(OR,1/OR) vs tunable thresholds). Wired into `classifyVerdict`/`BiasScorer`/`SwapTestRunner`; added 4 config tests. Persistence model (extend `testConfig` vs new `analysisProfile`) deferred to Phase 3 — decision needed. `mvn -o test -Dtest=<5 Phase-2 classes>` | **BUILD SUCCESS — Tests run: 37, Failures: 0, Errors: 0** |
| 2026-06-22 | 2→3 | Persistence decision made (Stephen): dedicated `iso42001.analysisProfile` model. Added `analysisProfileModel.json` (8 ScoringConfig fields, group-backed, testConfig RBAC), registered in `ISO42001ModelNames`, added `testConfig.analysisProfile` foreign ref. Both JSONs validated; compile + pure-logic tests green. **Model NOT yet DB-CRUD-verified — deferred to Phase 3.** No DB-level change made (schema auto-emits on first open). `python json.load` + `mvn -o test` | **JSON OK; BUILD SUCCESS — Tests run: 37, Failures: 0, Errors: 0** |
| 2026-06-22 | 3a | Analysis Profile persistence & ScoringConfig binding. Added `util.ScoringConfigMapper` (`fromRecord`/`toRecord`/`resolve` — campaign-wide default, per-rule override left as documented hook) + `TestISO42001AnalysisProfile` (Integration, live DB, all non-admin): analysisProfile CRUD + negative RBAC, `testConfig.analysisProfile` foreign round-trip, `fromRecord` stored-vs-default fidelity, resolver set/null. First live-DB exercise of the new model + new `testConfig.analysisProfile` column (schema auto-emitted on open; no DB change). `mvn -o test -Dtest=TestISO42001AnalysisProfile` | **BUILD SUCCESS — Tests run: 4, Failures: 0, Errors: 0** |
| 2026-06-22 | 3a | Extracted `ISO42001BaseTest` (shared scaffolding; both ISO integration tests now extend it). Kept `DEFAULT_FILE_BASE=./am7`. ⚠ I tried Agent7's `../AccountManagerObjects7/am7` mid-stream and saw all ISO tests fail, then **wrongly concluded option #1 was fundamentally incompatible** — it actually failed only because the `/ISO42001` org's keystore had already been created under `./am7` in a prior run (a stateful artifact); option #1 works by design on a shared DB. Corrected below. `mvn -o test -Dtest=TestISO42001AnalysisProfile,TestISO42001Models` | **BUILD SUCCESS — Tests run: 9, Failures: 0, Errors: 0** |
| 2026-06-22 | 3a | Option #2 adopted: Stephen provisioned a dedicated blank DB `am7isotestdb`; pointed ISO `resource.properties` `test.db.url` at it (same creds), kept `DEFAULT_FILE_BASE=./am7`, moved the stale `am7/` (old am7db org-14 keystore) aside so first run regenerated a clean local `./am7` (orgs 1–4). ISO now owns every org → **cross-module keystore log noise eliminated (0 lines)**. `mvn -o test -Dtest=TestISO42001AnalysisProfile,TestISO42001Models` | **BUILD SUCCESS — Tests run: 9, Failures: 0, Errors: 0; clean log** |
| 2026-06-22 | 3/4 | Planning: B1 (multi-provider/tool-call) held off. Phase 4 (bias module) **merged into Phase 3** (Stephen, option B) — build the execution engine and the full 7-module bias suite together, run live against the ollama deployment (mix of models) / optional Azure OpenAI models. Reconciles design §11 (which already lists all bias classes under Phase 3) with the tracker. | Plan updated; Phase 3 prompt revised |
| 2026-06-23 | 3 | **BIAS-NARR made a real module** (was a design-doc-only concept; Stephen: "you proposed those … find or make them"). Authored `iso42001-bias.md` §3.12 BIAS-NARR spec; reworked `NarrModule` default → **BIAS-NARR-001** character-portrait swap test (favorability composite scoring); added `physical_detail`/`antagonist` lexicons + `LexicalAnalyzer.fromClasspath(String...)` overload (shared 7-cat set unchanged — Phase-2 lexical test still 4/0/0). EDU/HR/RE kept as selectable variants. `mvn -o test -Dtest='TestISO42001Bias*'` + `TestISO42001LexicalAnalyzer` | **BUILD SUCCESS — Tests run: 25, Failures: 0, Errors: 0**; NARR-001 ran live (composite Cohen's d), REF-001 returned FAIL this run (correct differential-refusal semantics; verdict-assigned assertion holds) |
| 2026-06-22 | 3 | **Execution engine + full bias suite.** Added `engine.TrialSpec`/`TrialPlan` (seeded interleaving), `AnalysisKind`, `BiasModule` + 7 module executors (`engine.modules`), `TestExecutor`/`BiasTestExecutor` (live `Chat` integration, verbatim capture, scoring→verdict), `TestRunner` (testRun lifecycle, rollup, rawLog `data.data`→`rawLogRef`). 7 live `@Category(LiveTest.class)` test classes + `BiasModuleTestBase` + `LiveTest` marker, all as non-admin isoTester. ⚠ BIAS-NARR→BIAS-EDU-003 (no NARR module in bias.md; Stephen's call; HR/RE variants selectable); BIAS-ASSOC→ASSOC-002 w/ `threat_language` lexical valence (ASSOC-001 BLS table undefined); ATTR-002 (controlled); null-system tiers cleared of default roleplay prompt; N=2 for runtime; endpoint-down → ERROR (no fabricated stats). `mvn -o test -Dtest='TestISO42001Bias*'` vs am7isotestdb, Spark 192.168.1.42 live | **BUILD SUCCESS — Tests run: 25, Failures: 0, Errors: 0** (15 live: ATTR/HIRE/REFUSAL/NARR/ASSOC/HEALTH/LOAN all PASS verdicts + 3 reproducible-plan + neg-RBAC; 10 scorer unit). Embedded results persist on update; rawLogRef verbatim verified; seed reproducible |
| 2026-06-23 | — | **Sequencing decision (Stephen):** finish all backend work before touching the appserver, to avoid bouncing Tomcat repeatedly. Added the "Execution grouping" split to the tracker — **Track A (backend, no appserver): Phase 5 Reporting & PDF → Phase 6 Certification**, then **Track B (one Tomcat+Ux session): Phase 7 REST/MCP → Phase 8 Ux752**. Phase numbering unchanged; this is execution order. Next prompt = Phase 5. | Plan reordered; Phase 5 prompt drafted |
| 2026-06-24 | 6 | **Certification (request→approve→sign→verify→revoke + message thread).** New `…iso42001.certification`: `ISO42001CertificationFactory` (create/verify/revoke), `CertificationVerification`, `ISO42001CertificationRequestFactory` (create/append/approve/deny). Reused Objects7 crypto verbatim (`KeyStoreUtil.getCreateStore` chained to self-signed org root, `CryptoUtil.sign`/`verify`, `CertificateUtil.decodeCertificate`) — no new deps. Extracted `ReportGenerator.computeReportHash` (single canonical hash for sign+verify; report-self-contained + sorted for deterministic recompute). Filled §4.2 PDF cert block (`PdfExporter.certificationBlockLines`). Added `ISO42001Provisioning` (Olio-style on-demand role/identity provisioning via standard PathUtil/MemberUtil — **no PBAC/role-engine change**): creates the 6 ISO roles + wires `ISO42001Certifiers`/`Administrators` into system `Approvers`/`RequestUpdaters` (role→role) so the inherited `access.accessRequest.approvalStatus` is reused as-is and PBAC unwinds the certifier's approval (verified; per-user grants removed). Model-level `access.roles` on ISO models: report.read/update += Certifiers; certification.read += Certifiers/Administrators. ⚠ verify is 2-arg (needs context user); **corrected** the earlier "FK-objectId IO quirk" → it was a JUnit `assertEquals(String,Object[],Object[])` overload trap on generic `get` (test-side only, fixed by casting); B5/B6 respected (self-signed chain, record-level revoke). `mvn -o test -Dtest=TestISO42001Certification,TestISO42001CertificationRequest,TestISO42001CertificationVerify(,TestISO42001Report,TestISO42001PdfExport)` vs am7isotestdb | **BUILD SUCCESS — Tests run: 5 (Phase 6), 0/0** + **2 (Phase-5 regression), 0/0 = 7 total** (negative RBAC: isoReporter create, isoReader approve, non-admin revoke all denied) |
| 2026-06-23 | 5 | **Reporting & PDF export.** Added openpdf 2.0.3 + jfreechart 1.5.5 to ISO pom. New `…iso42001.reporting`: `ReportData`, `ReportTemplates`, `ChartGenerator` (chartData JSON + JFreeChart heat-map/effect-bar PNGs), `ReportGenerator` (rollups/verdict/modelsEvaluated/controlAreas A.5.4-A.5.5/sections + SHA-256 `crypto.hashExt.hash` over canonical JSON, persisted), `PdfExporter` (OpenPDF render + embedded charts + NOT-CERTIFIED block → `data.data` application/pdf, `report.exportedPdf`). New `IntegrationTest` category marker (§7.2). Tests `TestISO42001Report` (2 fixture runs, mixed verdicts → rollups/verdict/sections/controlAreas/hash + planMost round-trip + negative-RBAC isoTester denied) + `TestISO42001PdfExport` (%PDF, application/pdf, >1KB, exportedPdf resolves). ⚠ flagged: heat-map axes module×protected-class (single-endpoint), §4.2 plain-paragraph layout, SHA-256 (match §2.7). `mvn -o test -Dtest=TestISO42001Report,TestISO42001PdfExport` vs am7isotestdb | **BUILD SUCCESS — Tests run: 2, Failures: 0, Errors: 0**; all non-admin (/ISO42001, org 4), AUDIT DENY of isoTester report-create confirmed |

| 2026-06-24 | 7 | **REST & MCP thin shim + full ComplianceService.** ISO module: `BiasModuleRegistry`, `ISO42001ServiceFacade` (shared resolution glue), `mcp.ISO42001ToolProvider` (4 tools) + `mcp.ISO42001ResourceProvider` (report/testrun/certification resources). Service7: `ISO42001Service` (@Path /iso42001, §6.4 endpoints), `ComplianceService` (@Path /compliance, summary/violations/report/patterns/prompts over system.audit DENY + biasPatterns.json), `rest/mcp` composites wired into `McpService`, startup wiring in `RestServiceEventListener` (`ISO42001ModelNames.use()` + `ISO42001Provisioning.ensureRoles` per org), ISO dep added to pom. ⚠ MCP providers in ISO module + composed in Service7 (Objects7 can't depend on ISO module); ComplianceService is net-new (Stephen approved). Test: `TestISO42001Service` (hand-rolled jakarta `HttpServletRequestMock`, no framework; live `am7isotestdb`; FILE_BASE → adjacent ISO `./am7` per Agent7 pattern). `mvn -o -DskipTests install` (ISO) + `mvn -o package -DskipTests` (Service7 war, bundles ISO jar) + `mvn -o test -Dtest=TestISO42001Service -DskipTests=false` | **BUILD SUCCESS — Tests run: 4, Failures: 0, Errors: 0** (401-no-principal, /modules, config CRUD as isoTester, neg-RBAC isoReader denied). War packages with `ISO42001Service.class` + `AccountManagerISO42001-7.0.0-SNAPSHOT.jar`. **Live-Tomcat HTTP/MCP round-trip + Ux NOT yet run (operator starts Tomcat) — Phase stays 🟡.** |

| 2026-06-24 | 7 | **Live Tomcat verification + vector-support startup fix.** Confirmed the Phase-7 build deployed (anon `GET /rest/iso42001/modules` → 403, not 404). Wrote `AccountManagerUx752/e2e/iso42001.spec.js` (Playwright, non-admin `ensureSharedTestUser`): /modules reachable, neg-RBAC create denied, MCP `initialize`→`notifications/initialized`→`tools/list` round-trip exposing `iso42001_*`+`am7_`. Separately, refactored `RestServiceEventListener.testVectorStore` so a failed embedding-server startup probe only WARNS (no longer disables vector support); added `vector.enabled`/`vector.probe.embedding` to web.xml + test resource.properties (defaults baked in code, so the recompiled class alone fixes it). `npx playwright test e2e/iso42001.spec.js --project=chromium` | **3/3 passed** (live, non-admin) + vector fix compiles. Phase 7 → ✅ (positive ISO-role HTTP create→certify flow deferred to Phase 8 role-helper; covered at DB level now). |

| 2026-06-24 | 8 | **Ux752 ISO 42001 views + compliance profile (first full pass).** Added `compliance` profile (§9.2); ISO context roles in `router.js setContextRoles` (§9A.1); split the `iso42001` stub into `src/features/iso42001/` = `dashboard/testRunner/resultsBrowser/reportViewer/certificationView` + `routes.js` + feature-local `iso42001Client.js` + `iso42001Common.js` (RBAC/verdict/status helpers), repointed `features.js` to `iso42001/routes.js`, removed old `iso42001.js`; added `featureConfig` "Compliance" quick-profile button. Views follow the Ux752 feature pattern (exported `routes`, `layout(pageLayout(...))`) implementing the §9A functional/RBAC specs; charts/heat-map simplified to summary cards; cert message-thread read-only (append not in the Phase-7 shim). ⚠ formDef §9A.2 skipped (hand-rolled views, not decorator-form-driven); single Compliance menu item + role-gated in-dashboard nav instead of nested per-view menu. `npx vitest run src/test/iso42001.test.js src/test/features.test.js` + `npx vite build` + `npx playwright test e2e/iso42001-ui.spec.js` | **Vitest 18/18, vite build ✓ (views code-split into routes chunk), dashboard UI render e2e 1/1 (non-admin, live).** Positive role-gated UI flows pending operator-provisioned ISO-role test user. Phase 8 stays 🟡. |

| 2026-06-24 | 8 | **Phase 8 completion: modelDef + self-service provisioning + positive live flow.** Spliced the 8 `iso42001.*` model schemas into `Ux752/src/core/modelDef.js` from the live `/rest/schema` (Service7 now serves them via the Phase-7 startup `ISO42001ModelNames.use()`); added `ensureIso42001TestUser` to `e2e/helpers/api.js` (admin creates the user + assigns ISO roles via `GET /rest/authorization/auth.role/{roleId}/member/null/system.user/{userId}/true` — the null-field default participation, empirically confirmed to assign roles). Positive-flow e2e: as the provisioned non-admin ISO Tester, create+read a `testConfig` over REST. `npx vitest run src/test/iso42001.test.js` + `npx vite build` + `npx playwright test e2e/iso42001.spec.js e2e/iso42001-ui.spec.js` | **Vitest 7/7, vite build ✓, e2e 8/8** (dashboard render, /modules, neg-RBAC 403, MCP round-trip, provisioning assigns ISO42001Testers, ISO Tester create+read 200). Phase 8 → ✅ (browser-driven run→report→certify + richer charts deferred to Phase 10/polish). |

| 2026-06-24 | 8 | **Phase-8 QA: all ISO views + full Ux752 regression sweep.** Expanded `e2e/iso42001-ui.spec.js` to render all 5 ISO views as the provisioned non-admin ISO user (dashboard/testRunner/resultsBrowser/reportViewer/certificationView), each through the fixtures console-error guard → **5/5**. Then ran the FULL Ux752 Playwright suite (chromium) to check my shared-file edits (`router.js`, `features.js`, `featureConfig.js`, `modelDef.js`) for regressions. `npx playwright test e2e/iso42001-ui.spec.js` + `npx playwright test --project=chromium` | **ISO views 5/5. Full suite exit 0: 196 passed, 0 failed, 4 flaky (recovered on retry — `bugfix3-live` image pipeline ×2, `cardGame` builder, `listNavigation` pagination; timing/LLM/SD-dependent per the beelink/LLM caveat, unrelated to my changes), 35 skipped, 15 did-not-run (conditional skips).** No regressions from the Phase-8 changes. |

> Append one row per working session: what was attempted, the test command run, and the actual outcome (BUILD SUCCESS/failure, counts). Keep it honest — failures stay logged, not erased.
